/**
 * 짤 공장 개미의 말풍선.
 *
 * 앱 말풍선(`speech-lines.ts`)과 **목소리는 같고 축이 다르다** — 저쪽은 손익을 시급으로
 * 나눈 시간(`nudgeTier`)을 따라 세기가 갈리지만, 여기는 손익도 시급도 없다. 짤은
 * 장면이 곧 상황이라 **장면마다 풀 하나**로 끝난다.
 *
 * 그래서 구간도 손익 방향도 없다. 대신 지켜야 하는 건 둘이다.
 *
 * 1. **장면끼리 겹치지 않는다.** 세 장면은 한 화면에서 탭으로 오가므로, 파는 개미와
 *    우는 개미가 같은 말을 하면 문구가 몇 개 없어 보인다.
 * 2. **앱 말풍선과도 겹치지 않는다.** 같은 개미의 같은 자리(머리 위)라, 결과 화면에서
 *    본 말을 짤에서 또 만나면 한 벌을 돌려쓰는 것으로 읽힌다.
 *
 * 둘 다 `pnpm --filter @yca/web lines`가 검사한다.
 *
 * **말은 짧아야 한다 (14자 안쪽).** 9:16 한 칸에 한 줄로 들어가야 하고, 줄이 넘치면
 * 말풍선이 개미보다 커진다. 세로 판형이라 가로로 도망갈 자리가 없다.
 *
 * 이 파일의 글자도 픽셀 폰트 서브셋 범위다 (`speech-lines.ts`와 함께 굽는다) —
 * 문구를 고치면 `pnpm --filter @yca/web font`을 다시 돌린다.
 */

export type MemeSceneId =
  | "dig"
  | "ride"
  | "flood"
  | "face"
  | "cushion"
  | "coaster"
  | "train"
  | "rocket"
  | "zen"
  | "stoic"
  | "hodl"
  | "wallet"
  | "pillar"
  | "water"
  | "block"
  | "polar"
  | "stair"
  | "grind"
  | "phone"
  | "storm"
  | "board"
  | "world"
  | "bounce";

/**
 * 문구 풀의 이름. **장면 하나가 풀을 둘 이상 쓸 수 있다** — 만원 열차는 봉이 오를 때와
 * 내릴 때 하는 말이 달라야 해서, 장면 안에서 풀을 갈아 끼운다. 로켓도 같다 —
 * 오르는 동안(`rocket`)과 떨어지는 동안(`rocketDown`)의 말이 다르다.
 */
export type MemeLinePool =
  // 안 울어·불기둥·물타기·폭풍 존버·세계의 개미들·반등은 여기 없다 — 대본이 정해진 판이라 `*_SCRIPT`를 순서대로 쓴다.
  | Exclude<
      MemeSceneId,
      "stoic" | "pillar" | "water" | "polar" | "stair" | "storm" | "world" | "bounce"
    >
  | "trainUp"
  | "trainDown"
  | "rocketDown"
  | "walletEmpty"
  | "blockLevel"
  | "boardDown"
  | "boardDoze";

/**
 * 1. 땅파기 — **왜 파고 있는지**를 답하는 줄이다. 해는 쨍쨍하고 땀은 흐르는데
 * 개미는 계속 판다. 이유가 딱하고 구체적일수록 웃긴다 (카드값·전세·평단).
 */
export const DIG_LINES: readonly string[] = [
  "이번 달 카드값이야",
  "평단 낮추려면 파야 해",
  "굴 전세가 올랐대",
  "여왕개미가 시켰어",
  "금이라도 나와라",
  "커피는 끊었어",
  "월요일부터 파는 중",
  "물린 주식 값이야",
  "여기 어딘가 내 돈이",
  "쉬면 굶어",
];

/**
 * 2. 탑승 — 산·구름·우주를 지나며 신이 난다. **고도가 올라갈수록 말도 붕 떠야** 해서,
 * 밑도 끝도 없는 낙관과 슬쩍 새어나오는 불안을 섞는다.
 */
export const RIDE_LINES: readonly string[] = [
  "유후 올라간다",
  "이거 어디까지 가",
  "안전벨트가 없는데",
  "구름 위다 구름",
  "달까지 가보자",
  "익절은 언제 하지",
  "고소공포증 나았어",
  "여왕개미 보고 있나",
  "손 흔들어 저 아래로",
  "내려달란 말 아니야",
];

/**
 * 3. 눈물바다 — 다 잃었다. **금액을 적지 않는다** (짤에는 숫자가 없고, 이 앱은 금액을
 * 화면 밖으로 안 내보낸다). 잃은 크기는 물이 차오르는 높이가 말한다.
 */
export const FLOOD_LINES: readonly string[] = [
  "다 잃었어 진짜 다",
  "전재산이 녹았어",
  "이거 다 내 눈물이야",
  "적금 깬 돈이었는데",
  "겨울 양식이 없어졌어",
  "여왕개미 볼 낯이 없어",
  "물 좀 퍼내줘",
  "굴까지 잠겼어",
  "숨 쉴 곳이 없어",
  "여기서 헤엄쳐 나가야 해",
];

/**
 * 4. 얼굴 클로즈업 — 화면에 얼굴밖에 없다. **말이 상황을 혼자 설명해야** 하므로,
 * 눈물바다(다 잃은 뒤)와 달리 **떨어지는 중**을 말한다: 지금 파랗고, 지금 녹고 있다.
 *
 * 여기만 **개미 살림이 아니라 주식 용어로** 말한다 (저점·반등·물타기·손절). 얼굴이
 * 화면을 채워 배경이 상황을 못 알려주므로, 말이 "무엇 때문에 우는가"를 대신 세운다.
 */
export const FACE_LINES: readonly string[] = [
  "저점이 대체 어디야",
  "계좌가 녹아내려",
  "차트를 못 보겠어",
  "반등은 언제 오는데",
  "물타기도 못 하겠어",
  "여기가 바닥인 줄",
  "손절 각인가 이거",
  "빨간 날이 그리워",
  "파랗게 물들었어",
  "장 열리자마자 이래",
];

/**
 * 5. 돈방석 — 자랑이다. **액수는 말하지 않는다** (이 앱은 금액을 화면 밖으로 안 내보내고,
 * 짤에는 숫자가 없다). 얼마인지 대신 **얼마나 여유로운지**로 자랑한다.
 */
export const CUSHION_LINES: readonly string[] = [
  "훗 별거 아니야",
  "이게 바로 복리지",
  "돈방석 푹신하네",
  "앉을 자리가 넉넉해",
  "여왕개미도 부럽대",
  "일개미 아니고 사장",
  "이 정도는 돼야지",
  "더 사둘걸 그랬나",
  "곳간이 꽉 찼어",
  "빨간불이 예쁘다",
];

/**
 * 6. 롤러코스터 — 여러 마리가 함께 탄다. **개미 하나의 혼잣말이 아니라 서로 지르는 소리**라
 * 명령형이 많다 (붙잡아라·물타라·불타라). 장 시작부터 끝까지가 한 바퀴다.
 */
export const COASTER_LINES: readonly string[] = [
  "장 시작한다",
  "꽉 붙잡아",
  "물타라 물타라",
  "불타라 불타라",
  "손 놓으면 끝이야",
  "내려주세요 제발",
  "올라간다 올라가",
  "안전바는 어딨어",
  "다들 살아있지?",
  "이번 칸은 파랗다",
];

/**
 * 7-1. 만원 열차 — 역과 탑승. 아직 차트에 오르기 전이라 **정류장에서 할 말**이다
 * (자리·손잡이·막차). 종목 이름은 간판이 말해주므로 문구는 거들지 않는다.
 */
export const TRAIN_LINES: readonly string[] = [
  "이 열차 맞나요",
  "자리 없어도 타",
  "밀지 마 밀지 마",
  "놓치면 후회해",
  "다 탔으면 출발",
  "손잡이 꽉 잡아",
  "만석입니다",
  "막차는 아니겠지",
];

/**
 * 7-2. 양봉 구간 — 열차가 오르막을 달린다. **신났지만 아직 안 내렸다**는 게 이 풀의
 * 목소리다 (익절 얘기는 안 한다 — 그건 내리는 사람 말이고, 여긴 다 타고 있다).
 */
export const TRAIN_UP_LINES: readonly string[] = [
  "이 맛에 탄다",
  "가즈아",
  "역시 타길 잘했어",
  "더 밟아 주세요",
  "창밖 좀 봐",
  "이대로만 가자",
  "고점은 아직이야",
  "환승 안 해",
];

/**
 * 7-3. 음봉 구간 — 내리막. **내리고 싶지만 열차는 안 선다.**
 */
export const TRAIN_DOWN_LINES: readonly string[] = [
  "어어 내려간다",
  "브레이크 좀요",
  "여기서 내릴게요",
  "손잡이 놓치겠어",
  "이번 역 어디야",
  "다시 올라가겠지",
  "속이 안 좋아",
  "왜 후진해",
];

/**
 * 8-1. 로켓 발사 — 상승. 미장 로켓 코에 셋이 올라타고 화성으로 간다. **연료가 넉넉한 줄
 * 아는 목소리다** — 밑도 끝도 없는 확신일수록 뒤의 추락이 웃긴다.
 */
export const ROCKET_LINES: readonly string[] = [
  "와- 화성가자!!",
  "지구야 안녕",
  "달은 그냥 지나쳐",
  "이-얏호! 영차 영차!",
  "스엑 발사 !! 화성 가자",
  "별이 손에 잡힐 듯",
  "정거장 없이 직행",
  "여왕개미도 태울걸",
];

/**
 * 8-2. 로켓 하강 — 추진이 꺼졌다. **긴 말을 못 한다.** 떨어지는 중이라 짧은 탄식과
 * 다급한 한마디뿐이다.
 */
export const ROCKET_DOWN_LINES: readonly string[] = [
  "아...",
  "어? 어어??",
  "연료가 없대",
  "추진이 꺼졌어",
  "어디까지 내려가는 거예요?",
  "누가 좀 잡아줘",
  "이거 환불 되나",
  "내리막은 빠르네",
];

/**
 * 9. 무념무상 — 요동치는 차트 앞에서 눈만 껌뻑이는 얼굴. **밈의 대사 그대로 무심해야**
 * 한다 — 놀라거나 우는 말이 섞이면 표정과 어긋난다. 결론은 늘 관망이다.
 */
export const ZEN_LINES: readonly string[] = [
  "나는 아무런 생각이 없다.",
  "기회를 기다리자.",
  "생각이 없기 때문이다.",
  "무념무상",
  "존버는 승리한다",
  "차트는 차트고 나는 나다",
  "눈 뜨면 올라 있을 거다",
  "오늘도 관망한다",
];

/**
 * 10. 안 울어 — 하강 차트 앞에서 눈물이 고인다. **울고 있다고 말하지 않는다** — 화면이
 * 이미 그렁한 눈을 보여주므로, 말은 그걸 부정하는 쪽이라야 웃긴다. 무념무상(관망)과
 * 달리 여기는 **버티는 중**이다.
 *
 * **이 판만 풀이 아니라 대본이다** — 적힌 차례대로 네 컷이 뜬다. 앞뒤가 이어지는 말이라
 * ("뭔가 잘못됐어" → "단단히") 순서가 곧 농담이고, 뽑기에 맡기면 뒷말이 먼저 뜨거나
 * 한 컷이 통째로 빠진다. 그래서 `MEME_LINES`에 없고 `다시 뽑기`도 이 판은 안 바꾼다.
 *
 * 목소리는 **회의록처럼 담담하다.** 비명도 감탄사도 없이 사태를 보고하고, 강조어를
 * 문장 뒤로 흘려보낸다. 웃음은 여기서 나온다 — 얼굴은 그렁한데 말은 사무적이라 둘이
 * 어긋난다. 마지막 줄이 결론을 안 내는 것도 같은 이유다: 울지도 팔지도 않고 그냥 기다린다.
 */
export const STOIC_SCRIPT: readonly string[] = [
  "뭔가 잘못됐어",
  "단단히",
  "생각보다 심각하네 이거",
  "일단 기다려 본다",
];

/**
 * 12. 불기둥 — **하루가 흐르는 판이라 대본이다.** 아침 장(9시) → 장중 → 종가 순으로
 * 여섯 컷이 지나가고, 컷마다 봉이 하는 일이 다르다 (솟고 · 전고점 · 꺾이고 · 기준선을
 * 뚫고 · 창 밖으로 · 사라진 뒤). 순서가 곧 하루라 뽑기에 맡길 수 없다 — 종가 얘기가
 * 첫 컷에 뜨면 하루가 거꾸로 간다.
 *
 * 목소리는 **장 보는 개미의 실시간 반응**이다. 아침엔 느낌표가 붙고, 꺾이는 순간엔
 * 봉한테 말을 걸고("야 너 어디가" — 이 판에만 있는 2인칭이다), 종가에서는 문장이
 * 짧아지다 말끝을 흐린다. 마지막 줄이 제일 짧은 게 이 대본의 결론이다.
 */
export const PILLAR_SCRIPT: readonly string[] = [
  "9시 시작이 좋아!",
  "전고점 돌파인가!",
  "점심 먹고 왔더니",
  "야 너 어디가",
  "종가 무슨일!??!",
  "아 망했..",
];

/**
 * 13. 물타기 — **대본이다.** 물을 주는 동안 네 컷, 마지막 클로즈업에서 한 컷.
 *
 * 네 컷이 순서대로 **애정 → 지출 → 의심 → 원망**으로 옮겨간다. 뽑기에 맡기면 원망이
 * 먼저 오고 애정이 나중에 와서, 같은 문장인데도 물을 계속 주는 이유가 안 보인다.
 *
 * 마지막 줄만 개미가 아니라 **새싹 위에** 뜨고, 말하는 이도 개미가 아니라 **새싹**이다
 * (그래서 "니 월급"이다). 클로즈업 컷이라 말풍선이 화면 가운데를 차지하므로 접히는
 * 모양까지 못 박아 뒀다 — 이 줄에만 줄바꿈(`\n`)이 들어 있는 이유다.
 *
 * 이모지는 셋뿐이다 — **하트(애정) · 돈(지출) · 새싹(결론)**. 글에는 이모지 글자를 그대로
 * 적지만 **화면에는 도트로 그린 그림글자가 나간다** (`meme-bubble.ts`의 `ICONS`) — 픽셀
 * 폰트에 없는 글자라 시스템에 맡기면 빈칸이 되기 때문이다.
 */
export const WATER_SCRIPT: readonly string[] = [
  "내 주식 작고 귀여워 ❤️",
  "이번 달 월급도 부었어 💰",
  "도대체 언제 자라니?",
  "물을 언제까지 먹을꺼니?",
  "아직 니 월급이\n더 필요해 🌱",
];

/**
 * 15. 남극 탐험 — **세 번 속는 대본이다.** 양봉을 찾아 달려가면 다 와서 음봉으로
 * 뒤집히고 얼음이 깨진다. 그걸 세 번 반복하므로 두 줄이 한 짝이고(찾음 → 속음),
 * 뽑기에 맡기면 속기도 전에 "또 속았다"가 뜬다.
 *
 * 짝마다 **기대는 세지고 반응은 짧아진다** — "찾았다" → "이번엔 진짜다!" → "지금이니!"로
 * 확신이 올라가는 만큼, 뒤따르는 말은 "착각했다" → "또 속았다"를 지나 마지막엔 말이
 * 아예 안 나온다. 세 번째 반응이 **모자이크**인 이유다: 개미가 뭐라고 했는지는 적지
 * 않고 가려버리는 쪽이, 무슨 욕을 골라 적든 그것보다 세다.
 *
 * 모자이크는 이모지 글자(`🤬`)로 적고 화면에는 도트 판때기가 나간다
 * (`meme-bubble.ts`의 `ICONS`) — 하트·돈·새싹과 같은 길이다.
 */
export const POLAR_SCRIPT: readonly string[] = [
  "찾았다 내 양식",
  "아 착각했다",
  "이번엔 진짜다!",
  "아 또 속았다",
  "지금이니!",
  "아 이 🤬🤬🤬",
];

/**
 * 16. 계단과 엘리베이터 — **격언을 다섯 컷으로 나눈 대본이다.** 오르는 동안 둘, 타는
 * 동안 하나, 떨어지는 동안 하나, 내려서 하나. 순서가 곧 격언이라 뽑기에 맡길 수 없다.
 *
 * 앞 셋은 **느긋하다** (한 칸씩 · 좀 오르네 · 편하게). 그 여유가 뒤 둘과 부딪혀야
 * 웃긴다 — 개미는 끝까지 뭘 당하는지 모르고, 마지막 줄에서도 화를 내는 게 아니라
 * 지하가 있다는 걸 그제야 안다.
 */
export const STAIR_SCRIPT: readonly string[] = [
  "한 칸씩 오르는 거지",
  "오늘은 좀 오르네",
  "편하게 내려가자",
  "왜 이렇게 빨라",
  "지하가 있었어?",
];

/**
 * 19. 폭풍 존버 — **대본이다.** 폭풍이 몰아치는 다섯 컷이 순서대로 흐른다: 조짐 →
 * 버팀 선언 → 손해가 아니라는 다짐 → 뒤집혀 튀어 오르는 순간의 탄성 → 갠 하늘.
 * 뒤 두 컷은 앞 세 컷의 다짐이 시험받는 순간이라 순서를 뒤집으면 존버의 서사가 안 선다.
 *
 * **네 번째 컷("내가 말했지!! ❤️")은 균등 분할이 아니라 뒤집혀 봉이 튀어 오르는
 * 구간에 못박혀 있다** (`speakCuts`, 나머지 넷과 달리 자란 시간(`STORM_GROW_MS`)에
 * 맞춘 창이다) — 폭풍의 바람이 아니라 **막 솟구치는 봉을 보고 하는 말**이라서다.
 * 첫 컷도 균등 분할이 아니라 **음봉으로 바뀐 뒤부터**(`STORM_INTRO_END`)만 뜬다 —
 * 처음 양봉 위에 이 대사가 겹치면 아직 아무 일도 안 일어났는데 조짐을 말하는 셈이 된다.
 *
 * 목소리는 **점점 흔들리다 끝에 안도한다** — 처음엔 담담히 선언하고("그래도 안 팔아"),
 * 가운데서 스스로를 다잡고("안 팔면 손해 아님!!!"), 튀어 오르는 순간 자신만만해지고
 * ("내가 말했지!! ❤️"), 마지막에 웃는다. 안 울어(하강)와 존버 중(상승)이 각각
 * 담담함·안도로 나뉜 걸 한 판 안에서 순서대로 지나간다.
 */
export const STORM_SCRIPT: readonly string[] = [
  "오늘 심상치 않다",
  "그래도 안 팔아",
  "안 팔면 손해 아님!!!",
  "내가 말했지!! ❤️",
  "이게 존버의 맛이지",
];

/**
 * 14-1. **첫 세 상자를 박을 때마다 하는 말, 순서가 정해져 있다.** 음봉 → 더 긴 음봉 →
 * 작은 양봉으로 이어지는 미니 차트에 맞춰, 실망이 쌓이다 마지막에 엉뚱한 데서 신이
 * 난다 — 손실 중독의 그 감각이라 뽑기에 맡기지 않는다.
 */
export const BLOCK_TRIPLE_SCRIPT: readonly string[] = ["똥 밟았네", "뭐야 이거", "도파민이 부족해"];

/**
 * 21-대본. 일희일비 — **장 시작 두 컷.** 여는 말이라 뽑지 않는다: 개장하자마자 음봉 둘이
 * 찍히는 자리에 얹히므로 순서가 곧 이야기고("왜 이래" → "점심은 굶나"), 뽑기에 맡기면
 * 하루가 어떻게 시작했는지가 매 판 달라진다.
 */
export const BOARD_OPEN_SCRIPT: readonly string[] = ["오늘 시작부터 왜이래 ?", "오늘 점심 굶어야 하나?"];

/**
 * 21-큐. 일희일비 — **몸짓에 딱 붙는 한마디 둘** (하트를 날리는 순간 · 눈이 감기는 순간).
 * 말이 그림과 같이 나와야 해서 뽑기에 안 맡긴다 — 풀에 넣어두면 그 몸짓이 나온 판에서
 * 엉뚱한 말이 뜨고, 정작 이 말은 몇 판에 한 번만 나온다.
 */
export const BOARD_CUE_SCRIPT: readonly string[] = ["유-후 하트", "z..Z.z 졸리네"];

/**
 * 22. 세계의 개미들 — **나라마다 개인투자자를 부르는 이름이 다르다.** 한국은 개미, 중국은
 * 부추(韭菜), 일본은 메뚜기(イナゴ), 브라질은 정어리(sardinha), 미국은 유인원(ape),
 * 러시아는 햄스터(хомяк)다.
 *
 * **이름이 곧 그 나라 개미가 당하는 방식이라** 한마디도 거기서 나온다 — 부추는 잘려도 또
 * 자라고, 메뚜기는 떼로 몰리고, 정어리는 고래가 무섭고, 유인원은 뭉치고, 햄스터는 볼에
 * 쟁인다. 캐릭터마다 한마디가 못박혀 있으므로 풀이 아니라 **대본이다**: 순서가 곧
 * 로테이션 순서고, 뽑기에 맡기면 부추가 고래를 무서워한다.
 *
 * **햄스터 줄에만 줄바꿈이 박혀 있다.** 한 줄로 두면 15자라 말풍선이 화면을 넘고, 접는
 * 자리를 정해주면 "먹고" 뒤에 "또 먹고"가 오는 말맛도 산다.
 */
export const WORLD_SCRIPT: readonly string[] = [
  "오늘도 영차 영차",
  "털려도 계속 자라난다",
  "이 종목이 급등이래!",
  "호달달.. 고래 무서워",
  "여기로 모여! 우끼",
  "일단 먼저 먹고,\n이것도 먹고",
];

/**
 * 22-무대. 캐릭터 머리 위에 붙는 나라 이름 — 말풍선이 아니라 무대에 박히는 글자다
 * (`SceneLabel`). **그래도 이 파일에 둔다**: 픽셀 폰트 서브셋이 글자를 긁어가는 파일이
 * 여기라, `meme-scenes.ts`에 박으면 그 글자만 시스템 폰트로 떨어진다.
 * 순서는 `WORLD_SCRIPT`와 같다.
 */
export const WORLD_COUNTRIES: readonly string[] = [
  "한국",
  "중국",
  "일본",
  "브라질",
  "미국",
  "러시아",
];

/**
 * 22-무대. 그 나라가 개인투자자를 부르는 이름 — 화면 아래 땅에 박힌다. **국기·나라 이름과
 * 따로 두는 게 이 판의 전부다**: 위가 "어느 나라"라면 아래는 "거기선 뭐라 부르나"라서,
 * 둘이 한 줄로 붙으면 농담이 사라진다. 순서는 `WORLD_SCRIPT`와 같다.
 */
export const WORLD_NAMES: readonly string[] = [
  "개미",
  "부추",
  "메뚜기",
  "정어리",
  "유인원",
  "햄스터",
];

/**
 * 순서가 정해진 판. 검사기가 풀과 같은 규칙(글자 수·중복)을 여기에도 건다 —
 * 풀에서 빠져나간 문구가 검사도 같이 빠져나가면 안 된다.
 */
export const MEME_SCRIPTS: Readonly<Record<string, readonly string[]>> = {
  stoic: STOIC_SCRIPT,
  pillar: PILLAR_SCRIPT,
  water: WATER_SCRIPT,
  polar: POLAR_SCRIPT,
  stair: STAIR_SCRIPT,
  storm: STORM_SCRIPT,
  blockTriple: BLOCK_TRIPLE_SCRIPT,
  boardOpen: BOARD_OPEN_SCRIPT,
  boardCue: BOARD_CUE_SCRIPT,
  world: WORLD_SCRIPT,
};

/**
 * 17. 일개미 행진 — 차트 위를 오가는 개미들의 혼잣말. **한 마리의 대사가 아니라
 * 여럿이 동시에 떠드는 채팅창**이다 — 화면 가운데에 온 개미를 중심으로 최대 세
 * 마리가 각자 다른 줄을 뽑아 동시에 띄운다. 그래서 순서 있는 대본이 아니라 풀이고,
 * **개미 투자자들**이 실제로 할 법한 말(존버·물타기·손절·가즈아)로 채운다 — 이
 * 판만 사람의 투자 용어를 그대로 쓴다(다른 판은 개미의 몸·살림으로 에둘러 말한다).
 */
export const GRIND_LINES: readonly string[] = [
  "물렸다 진짜",
  "존버가 답이다",
  "가즈아!!",
  "손절각인가",
  "이번엔 다르다",
  "설거지 당했네",
  "물타야하나",
  "익절 언제하지",
  "여기가 바닥이다",
  "몰빵했는데 어쩌지",
  "형이 사면 올라",
  "단타 치자",
  "물량 던진다",
  "줍줍 가자",
];

/**
 * 18. 미장 개미 — 새벽 세 시, 잠 못 들고 휴대폰 불빛 속에서 하는 혼잣말. **잠과
 * 시세 사이에서 갈등한다**는 게 이 풀의 목소리다 — 존버·손절 같은 결론은 안 낸다
 * (그건 다른 판의 몫이다), 그냥 눈이 안 감긴다는 얘기뿐이다.
 */
export const PHONE_LINES: readonly string[] = [
  "딱 한 번만 더",
  "미장은 지금이지",
  "눈 감아도 차트가",
  "해뜨기 전엔 못자",
  "이러다 새벽 새워",
  "불 끄고 다시 켜",
  "오늘도 늦었다",
];

/**
 * 11. 존버 중 — 상승 차트 앞에서 새어 나오는 웃음. **긍정하면서 계속 들고 간다**는 게
 * 이 풀이다 — 익절 얘기는 안 한다 (파는 순간 존버가 아니다). 자랑하는 돈방석과 달리
 * 여기는 **버틴 게 옳았다**는 안도다.
 */
export const HODL_LINES: readonly string[] = [
  "거봐 오른다니까",
  "안 팔길 잘했지",
  "존버는 옳았다",
  "이제 시작이야",
  "기다린 보람 있네",
  "더 갈 거야 안 팔아",
  "웃음이 나오네",
  "버틴 사람이 이긴다",
];

/**
 * 9-1. 빈 지갑 — **지갑을 닫고 있을 때.** "있었는데요 없었습니다"의 앞쪽 절반이라
 * 전부 과거형이다. 얼마였는지는 말하지 않는다 (짤에 금액은 안 나온다).
 */
export const WALLET_LINES: readonly string[] = [
  "분명 있었는데",
  "어제까진 있었어",
  "여기 넣어뒀는데",
  "월급 들어왔었는데",
  "지갑에 있었지",
  "많지는 않았어도",
];

/**
 * 9-2. 빈 지갑 — **펼친 순간.** 뒤쪽 절반이라 전부 종결형이고, 짧을수록 세다.
 */
export const WALLET_EMPTY_LINES: readonly string[] = [
  "없었습니다",
  "텅 비었습니다",
  "먼지만 있었습니다",
  "아무것도 없네요",
  "하나도 없었습니다",
  "바람만 들었습니다",
];

/**
 * 14. 코인 상자 — **두 번째 상자 앞에서 하는 혼잣말.** 전부 물음표로 끝난다: 이번엔
 * 뭐가 나올지 개미가 묻는 말이고, 답하는 사람은 아무도 없다.
 *
 * **답을 아는 척하지 않는다** — "이제 오른다"고 단정하면 훈수가 되고, 훈수는 하단
 * 한마디의 몫이다. 여기 개미는 상자만 두드리는 중이라 아는 게 없다.
 */
export const BLOCK_LINES: readonly string[] = [
  "지금이니?",
  "이제 시즌 오는거니?",
  "더 사야하니?",
  "탈출해야 하니?",
  "이번엔 진짜니?",
  "너도 오르는거니?",
];

/**
 * 14-보스. 두 괴물이 각각 든 깃발 문구 — **전쟁이 먼저, 금리가 다음이다.** 대사 풀이
 * 아니라 말풍선 몸통 안에 그대로 박히는 글자라 뽑지 않고 순서대로 고정으로 띄운다.
 */
export const BLOCK_ENEMY_FLAG: readonly [string, string] = ["전쟁", "금리"];

/**
 * 14-보스. **괴물을 밟는 순간 개미가 하는 말** — 한 마리당 하나씩, 고정이다. 전쟁
 * 괴물에게는 "꺼져!", 금리 괴물에게는 "에잇!" — 둘 다 한 방에 끝나는 말이라 길게
 * 안 끈다.
 */
export const BLOCK_STOMP_TAUNTS: readonly [string, string] = ["꺼져!", "에잇!"];

/**
 * 14-보스. **시드를 먹고 커진 순간의 혼잣말.** 화면 가운데 뜨는 "LEVEL UP!"은
 * 게임 UI 글자라 다른 손이고, 이건 그 아래 개미 자신의 반응이다.
 */
export const BLOCK_LEVEL_LINES: readonly string[] = [
  "몸이 커졌어!",
  "이게 시드였구나",
  "힘이 솟아난다",
  "레벨이 올랐다",
  "더 세진 기분이야",
];

/**
 * 21-1. 일희일비 — **양봉이 찍힌 순간의 반응.** 스크린 위에 "일희일비 금지"가 걸려
 * 있는데 개미는 캔들 하나에 날뛴다는 게 이 판의 농담이라, 여기 말은 **표어를 정면으로
 * 어긴다** — 담담한 척하는 줄이 하나라도 섞이면 농담이 반으로 준다.
 *
 * 지금 눈앞의 봉 하나에만 반응한다 — 존버(`hodl`)처럼 버틴 보람을 말하거나
 * 돈방석(`cushion`)처럼 자랑하지 않는다. 그건 결과를 아는 개미의 말이고,
 * 이 개미는 다음 봉이 뭘지 모른다.
 */
export const BOARD_LINES: readonly string[] = [
  "올라간다 올라가!",
  "이래서 못 끊는다",
  "역시 내 안목이야",
  "심장이 뛴다",
  "빨간불 좋아 ❤️",
  "지금이라도 더 살까",
  "오늘 저녁은 고기다",
];

/**
 * 21-2. 일희일비 — **음봉이 찍힌 순간의 반응.** 위 풀과 한 짝이라 같은 자리에서
 * 뒤집힌 말을 한다 ("오늘 저녁은 고기다" ↔ "오늘 저녁은 굶는다"). 눈물바다처럼
 * 이미 잃고 난 뒤의 정서가 아니라 **지금 막 꺾이는 걸 보는** 다급함이다.
 */
export const BOARD_DOWN_LINES: readonly string[] = [
  "왜 내려가 왜",
  "심장이 내려앉네",
  "파란불 그만해",
  "잠깐 이게 아닌데",
  "다시 올려줘 제발",
  "앞이 안 보인다",
  "오늘 저녁은 굶는다",
];

/**
 * 21-3. 일희일비 — **횡보가 이어져 조는 동안.** 오르내림에 반응하던 개미가 할 일이
 * 없어진 자리라, 손익 얘기가 아예 안 나온다 — 지루하다는 말뿐이다. 표어를 지키고
 * 있는 유일한 구간인데 그게 **의지가 아니라 졸음**이라서 웃긴다.
 */
export const BOARD_DOZE_LINES: readonly string[] = [
  "아무 일도 안 나네",
  "이럴 거면 자야지",
  "장이 멈춘 것 같아",
  "심심해 죽겠어",
  "깨우면 말해줘",
  "횡보가 제일 힘들어",
];

/**
 * 21-무대. 스크린 위에 걸린 표어와 장 시작 전 시계 — 말풍선이 아니라 무대에 박히는
 * 글자다(`SceneLabel`). **그래도 이 파일에 둔다**: 픽셀 폰트 서브셋은
 * `speech-lines.ts` · 이 파일 · `share-copy.ts` 셋에서만 글자를 긁어가므로
 * (`scripts/build-font.mjs`), `meme-scenes.ts`에 박으면 `희`처럼 다른 데 안 쓰이는
 * 글자가 **폰트를 다시 구워도 시스템 폰트로 떨어진다.**
 *
 * 표어를 두 토막으로 나눈 건 **가운데 공백 때문에 글자 폭 합이 소수가 되어서**다 —
 * `drawLabels`는 가운데 정렬로 한 번에 찍으므로 폭이 정수가 아니면 모든 글자가
 * 소수 좌표에 앉아 픽셀 폰트가 흐려진다. 같은 이유로 "장 시작!"도 공백을 뺐다.
 */
export const BOARD_MOTTO: readonly [string, string] = ["일희일비", "금지"];
export const BOARD_CLOCK: readonly [string, string, string] = ["8:59", "09:00", "장시작!"];

/**
 * 세계의 개미들 · 유인원 컷 — 곁 원숭이들이 내는 소리. **주인공 말풍선과 다른 손이다**:
 * 이건 무대가 그린 작은 판때기에 얹는 글자(`SceneLabel`)라 뽑기 풀에 안 들어간다.
 * 공백을 뺀 것도 이유가 있다 — 글자 폭 합이 정수여야 픽셀 폰트가 안 흐려진다.
 */
export const APE_CHEER = "우끼우끼";

/**
 * 23-반등. **뽑지 않고 못박는다** — 이 판의 농담은 앞 6초를 진짜로 믿게 만든 뒤 카메라가
 * 물러나며 뒤집는 것이라, 앞말은 **한 점 의심도 없는 환희**여야 한다. 뽑기에 맡기면
 * 그중 하나만 미심쩍어도 반전이 죽는다.
 *
 * **뒤집힌 뒤에는 말이 없다.** 물러난 화면이 이미 다 말하고 있어서, 거기 한마디를 얹으면
 * 그림이 한 번 더 설명당한다 (남극 판이 마지막을 모자이크로 가린 것과 같은 자리).
 */
export const BOUNCE_SCRIPT: readonly [string, string] = ["떡상 시작이다!", "역시 내가 옳았어"];

export const MEME_LINES: Readonly<Record<MemeLinePool, readonly string[]>> = {
  dig: DIG_LINES,
  ride: RIDE_LINES,
  flood: FLOOD_LINES,
  face: FACE_LINES,
  cushion: CUSHION_LINES,
  coaster: COASTER_LINES,
  train: TRAIN_LINES,
  trainUp: TRAIN_UP_LINES,
  trainDown: TRAIN_DOWN_LINES,
  rocket: ROCKET_LINES,
  rocketDown: ROCKET_DOWN_LINES,
  zen: ZEN_LINES,
  hodl: HODL_LINES,
  wallet: WALLET_LINES,
  walletEmpty: WALLET_EMPTY_LINES,
  block: BLOCK_LINES,
  blockLevel: BLOCK_LEVEL_LINES,
  grind: GRIND_LINES,
  phone: PHONE_LINES,
  board: BOARD_LINES,
  boardDown: BOARD_DOWN_LINES,
  boardDoze: BOARD_DOZE_LINES,
};

/**
 * 장면과 박자(beat)에 맞는 한마디.
 *
 * `seed`는 한 번의 촬영을 붙잡아두는 값이다 — 같은 seed면 같은 영상이 나와야
 * **미리보기에서 본 그 말이 그대로 녹화된다.** 박자마다 다른 줄을 뽑되, 바로 앞
 * 줄과는 겹치지 않게 한 칸 밀어준다 (같은 말이 두 번 이어지면 말풍선이 안 바뀐 줄 안다).
 */
export function pickMemeLine(pool_: MemeLinePool, seed: number, beat: number): string {
  const pool = MEME_LINES[pool_];
  if (pool.length === 0) return "";

  const start = Math.abs(Math.floor(seed));
  const index = (start + beat * step(start, pool.length)) % pool.length;

  return pool[index] ?? "";
}

/**
 * 박자마다 몇 칸씩 건너뛸지. **풀 길이와 서로소인 수**여야 한 바퀴를 다 돌기 전에
 * 같은 줄로 되돌아오지 않는다 — 10줄짜리 풀에 5칸씩 뛰면 두 줄만 번갈아 나온다.
 */
function step(seed: number, length: number): number {
  if (length <= 2) return 1;

  for (let offset = 0; offset < length; offset += 1) {
    const candidate = 1 + ((seed + offset) % (length - 1));
    if (gcd(candidate, length) === 1) return candidate;
  }

  return 1;
}

function gcd(a: number, b: number): number {
  return b === 0 ? a : gcd(b, a % b);
}
