# Yung-chaAnts (영차Ants)

내가 산 주식의 평가손익을 **내 시급으로 환산해서 "몇 시간 더 일해야 본전인지"** 보여주는
모바일 웹. 손익 상태는 픽셀아트 개미의 상태로 표현된다.

pnpm 워크스페이스 모노레포. 웹(Next.js) + API(Fastify) + 둘이 공유하는 계약(zod 스키마).

## 도메인

핵심 흐름은 **세전 연봉 → 실수령 시급 → 손익 → 시간**이다.

1. **세전** 연봉을 입력하면 **실수령** 시급이 나온다 (월 소정근로시간 209시간 기준).
2. 종목을 검색해 평단가와 보유 수량을 입력한다.
3. `(현재가 − 평단가) × 수량`을 시급으로 나눠 노동시간을 만든다.
   - 손실 → "출근 131번 만큼 더 일해야 해"
   - 수익 → "출근 131번 만큼 안 나가도 돼"
4. **장중 현재가는 아직 없다.** 현재가 자리에 전일 종가를 쓰고 화면은 "실시간 적용
   준비중"을 알린다. 카운터는 그 종가 기준 실제값으로 서 있고, 실시간 시세를 붙이면
   다시 1초마다 움직인다 (SSE 경로는 살아있다).

카운터는 세 줄이고 **날을 세는 건 첫 줄뿐이다.**

```
출근 131번 만큼                    ← formatWorkSpanWords()  "며칠치 노동인가"
다신 안 나가도 돼                   ← countdown-lines.ts
총 1,054시간 50분 34초 만큼 돈 벌었다  ← formatTotalWorkLine()  "몇 시간인가"
```

큰 글씨는 **"{출근 N번} 만큼"**이다. 1근무일(8시간, `WORK_HOURS_PER_DAY`)을 넘기면
"출근 131번", 그 아래는 "3시간 20분"·"23분"으로 적는다. **문장이 출근 은유를 쓰기
때문에 숫자도 근무일이어야 한다** — 24시간짜리 "43일"로 적고 뒤에 "안 나가도 돼"를
붙이면 43번 결근으로 읽히지만 실제로는 131번 출근이다. 출근 횟수는 반올림이 아니라
**버림**이다 — "그만큼 안 나가도 된다"고 말하는 줄이라 실제로 채운 횟수만 센다.

시계 줄은 **날로 접지 않고 시간을 끝까지 누적한다.** 여기 "1,054시간"은 잠자는 시간까지
흐르는 절대시간이 아니라 **일한 시간**이고, 그래서 문장이 "돈 벌었다 / 돈 잃었다"로
끝난다. 날 자리를 안 두는 게 핵심이다 — 위에서 이미 날을 세고 있어서 여기서도 세면
한 화면에 날짜가 두 번 적힌다. 자가 다르면 숫자가 어긋나 보이고(한때 "출근 132번" 밑에
"43일 22:50:34"가 붙어 3배 차이로 나란히 섰다), 자를 맞추면 같은 말을 두 번 한다.

시·분·초는 값과 상관없이 **늘 셋 다 적는다** (`총 0시간 06분 00초`). 자리가 사라졌다
생겼다 하면 1초마다 줄 폭이 널뛴다. 본전은 셀 시간도 벌거나 잃은 돈도 없어 이 줄이
통째로 빠진다 (`formatTotalWorkLine()` → null).

큰 글씨 둘째 줄은 **시간이 길수록 세지는 한마디**다 ("더 일하면 돼" … "인생을 갈아
넣어야 해", [countdown.tsx](apps/web/src/components/countdown.tsx)). 세기는 **일해야
하는 시간의 크기**로 정한다 (부정·긍정 모두, `nudgeTier` **7구간**) — 그 시간은 손익을
유저 시급으로 나눈 값이라 곧 "내 노동 몇 시간짜리 손익인가"다.
문구는 [countdown-lines.ts](apps/web/src/components/countdown-lines.ts)에 있고,
**이 풀에만 걸리는 두 규칙**이 있다 (앞에 시간이 붙는 줄이라서 생기는 제약).

1. **기간명사 금지** — 하루·며칠·반나절·평생·주말은 앞말을 딴 단위로 덮어쓴다.
   "43일 22시간 만큼 평생 안 나가도 돼"가 그래서 나왔다. 하단 한마디에서는 마음껏 쓴다.
2. **"나가다" 은유는 3구간부터** — 그 아래는 앞말이 "23분"이라 "안 나가도 돼"의 목적어가
   안 선다. 3구간부터는 앞말이 "출근 N번"이라 앞줄이 목적어를 받아준다.

둘 다 `pnpm --filter @yca/web lines`가 검사한다 (아래 "문구 검사").

시계 바로 아래엔 **손익 금액을 일상 물건으로 환산한 한 줄**이 랜덤으로 붙는다
("치킨 13마리 시킬 수 있어" / "점심을 햇반 41개로 버텨야 해",
[cost-lines.ts](apps/web/src/components/cost-lines.ts)). 개수는 `|손익| ÷ 물건값`이고
물건은 seed로 고른다 — 카운터·말풍선과 같은 `nudgeTier` seed라 구간·방향이 바뀔 때만
다시 뽑는다. **금액(₩)이 필요하므로 대시보드에서만 뜬다** — 공유 화면은 금액을 안 싣기에
`Countdown`에 `pnl`을 안 넘기고, 그러면 이 줄도 없다 (개인정보 규칙과 맞물린다).

계산은 전부 [packages/shared/src/status.ts](packages/shared/src/status.ts)에 있다.
화면은 이 함수의 결과만 그린다 — 컴포넌트 안에서 손익을 다시 계산하지 않는다.

### 세전 입력, 실수령 환산

**입력받는 값은 언제나 세전 연봉이고, 나누는 값은 언제나 실수령 시급이다.**
통장에 안 들어오는 돈으로 나누면 "몇 시간 더 일해야 하는지"가 체감과 어긋난다.
`profile.annualSalary`에는 세전이 저장되고, 손익 환산 직전에 `netHourlyWage()`가
실수령으로 바꾼다 ([packages/shared/src/wage.ts](packages/shared/src/wage.ts)).

공제는 **1인 가구(본인 기본공제만) · 비과세 수당 없음** 기준 추정치다 — 4대보험
근로자 부담(국민연금·건강·장기요양·고용)을 빼고, 근로소득공제 → 과세표준 →
누진세율 → 근로소득세액공제 → 지방소득세 순으로 소득세를 뺀다. 부양가족이나
비과세 식대는 받지 않으므로 실제 실수령액과는 차이가 난다. 화면에도 추정치임을 적어둔다.

요율은 **연도마다 바뀐다.** `wage.ts` 상단 상수(`RATE_YEAR`와 그 아래 요율들)만
고치면 되도록 한 곳에 모아뒀다 — 계산식 사이에 숫자를 흩뿌리지 말 것.
현재 값은 2026년 기준(국민연금 근로자 4.75%, 기준소득월액 41만~659만원,
건강보험 3.595%, 장기요양 건보료의 12.95%, 고용보험 0.9%, 최저시급 10,320원).

**`annualSalary: 0`은 미입력이 아니라 "무일푼"이다.** 온보딩에서 "나는 그냥 무일푼이야"를
고른 상태이고, 이때 `effectiveAnnualSalary()`가 **최저임금 연봉**을 대신 끼워 넣는다.
0으로 나누면 시간이 무한대가 되고 `calcPositionStatus`가 그걸 0초로 떨어뜨려
카운터가 통째로 죽기 때문이다. 최저임금도 세전이라 연봉으로 되돌려 넣어 공제 경로를
그대로 태운다 — 여기서 시급을 직접 만들어 끼우면 실수령 환산을 우회하게 된다.
환산 기준이 내 시급이 아니므로 **화면에 "최저시급 기준"을 반드시 적는다**
(`isBroke()` → 카운터 위 캡션).

### 장대봉 무대

손익 시간은 화면 정중앙에서 자라는 **장대봉**으로 그린다
([apps/web/src/components/candle-scene.tsx](apps/web/src/components/candle-scene.tsx)).
손실이면 땅속으로(파랑), 수익이면 하늘로(빨강) 뻗는다.

세계 좌표는 **봉 폭 = 1**이다. 높이는 `candleScale()`이 주는 1~12배이고,
카메라는 그 높이가 무대에 들어오도록 픽셀 배율을 정한다.
**개미가 작아 보이는 건 이 줌아웃의 결과다** — 개미 크기를 따로 계산하지 않는다.
봉이 길수록 카메라가 빠지고, 개미는 같이 작아진다 (최소 크기 하한만 둔다).

봉 길이는 근무일 기준 로그 스케일이다. 4시간(0.5일) 이하는 1:1 정사각형 그대로고,
160시간(20일, 한 달치)에서 멈춘다. **시간 숫자는 clamp와 무관하게 실제 값을 표시한다** —
봉이 최대치에 닿아도 카운터는 계속 센다.

평단가를 받는 화면도 **같은 무대**를 쓴다 (`mode="waiting"`). 아직 손익이 없으니 봉은
그리지 않고, 개미가 1:1 크기로 좌우를 서성이며 대기용 말풍선을 띄운다. 입력을 위한
별도 레이아웃을 만들지 말 것 — 결과 화면과 갈라지는 순간 둘이 어긋난다.

등장 연출(씨앗 → 성장+줌아웃 → 개미 기어옴 → 손 흔들기 → 말풍선)은 **최초 1회만** 돈다.
이후 시세가 들어오면 봉 높이와 배율만 이어서 변한다 — 봉을 다시 그리거나 개미를
다시 기어오게 하지 않는다. 손익이 뒤집힐 때도 재생하지 않는다. 어차피 본전을 지나며
시간이 0에 수렴해 봉이 씨앗까지 줄었다가 반대로 자란다.

**수익은 다 그려지면 개미 쪽으로 줌인한다.** 봉은 바닥선(정중앙)에서 위로 뻗고 개미는
바닥선에 서므로, 수익이면 아래 절반(땅속)이 텅 빈다. 손실은 위 절반이 날씨(비·구름)로
차 있어 안 비지만 수익의 땅속은 빈 채로 남는다. 그래서 등장 연출이 끝나면(`phase === "wave"`
+ `mood === "profit"`) 카메라를 개미 쪽으로 밀어 그 빈 아래를 걷어낸다. **바닥선만 내리지
말 것** — 하늘/땅 경계 그라데이션이 바닥선과 어긋난다. 배경·날씨·봉·개미를 `.candle-world`
한 겹으로 묶고 거기에 트랜스폼(`translateY(25%) scale(1.28)`)을 걸어 **월드째** 움직인다.
`.candle-stage`는 그 카메라 프레임이라 바탕만 하늘색으로 깔아둔다 (줌으로 월드가 밀릴 때
위에 드러나는 띠가 이어지도록). 손실로 뒤집히면 트랜스폼이 풀려 도로 가운데로 돌아온다.

### 배경 날씨

`sceneLevelFromStage()`가 개미 50단계를 -3 ~ +3의 7레벨로 접는다. **새 축을 만들지 말 것** —
따로 두면 개미 껍질 색과 배경이 각자 논다.

| 레벨 | stage | 수익률 | 배경 |
| --- | --- | --- | --- |
| −3 | 0–10 | −30% 이하 | 먹구름 + 폭우 + 번개 |
| −2 | 11–17 | −28% ~ −16% | 먹구름 + 비 |
| −1 | 18–22 | −14% ~ −6% | 먹구름 |
| 0 | 23–27 | −4% ~ +4% | 없음 |
| +1 | 28–32 | +6% ~ +14% | 반짝이 |
| +2 | 33–39 | +16% ~ +28% | 폭죽 약 (2발) |
| +3 | 40–49 | +30% 이상 | 폭죽 강 (4발) + 색종이 |

본전 근처(레벨 0)를 ±4%로 넓게 비워둔 건 의도다. 좁게 잡으면 그 언저리에서 비가 왔다
폭죽이 터졌다 하며 깜빡인다. 같은 이유로 `sceneLevelWithHysteresis()`가 경계에서
한 단계를 더 요구한다.

**날씨는 전부 `.scene-sky`(위쪽 절반) 안에서만 그린다.** 땅속에 비가 내리면 이상하다.

### 개미 50단계

수익률을 50단계로 나눈다. **25단계가 본전이고 한 단계는 수익률 2%p**
(표현 범위 -50% ~ +48%, 바깥은 clamp). 단계가 오를수록 껍질이 창백한 회갈색(탈진) →
윤기나는 붉은 불개미로 연속 보간되고, 더듬이도 처짐 → 중립 → 쫑긋으로 바뀐다.
색은 보간이라 계단이 안 보이고, 형태는 3단으로 끊어 변화를 눈에 띄게 한다.

스프라이트는 **옆뷰**다 (오른쪽을 봄, `flip`으로 뒤집는다). 앱이 쓰는 자세는
`crawl1/crawl2`(기어가기), `stand`, `wave1/wave2`(손 흔들기) 다섯 프레임이고,
짤 공장이 `dig1/dig2`(파기) · `cry1/cry2`(울기) · `lookUp/lookDown`(올려다보기·내려다보기) ·
`doze1/doze2`(꾸벅꾸벅 졸기) · `jump`(뛰기 — 마리오처럼 **한 손을 든다**) ·
`shoot`(하트 발사) · `prone`(엎드리기) ·
`back1/back2`(등지고 걷기) · `backFlail1/backFlail2`(빠져서 버둥거리기)를 더 쓴다 —
**스무 자세가 모두 같은 16×16 문자맵에서 나온다.** 얼굴만 크게 잡는 클로즈업 짤은 예외로 **48×48 정면 얼굴 맵**을 따로 두지만
(아래 "짤 공장"), 그것도 이 파일 안에 있고 색은 같은 계산에서 갈라진다.
새 자세도 [ant-sprite.tsx](apps/web/src/components/ant-sprite.tsx)에 둘 것:
몸을 딴 파일에서 다시 그리면 앱 개미와 짤 개미가 서서히 다른 벌레가 된다.
팔(`w`)은 **가슴 색**으로 칠한다 — 다리 색(어두움)으로 칠하면 머리 옆에서 더듬이 한 짝처럼 보인다.

**뒷모습 넷만 옆뷰가 아니다** (`back*`). 카메라를 등지고 멀어지는 판(남극 탐험)이 쓰는
자세라 위에서부터 더듬이·머리·가슴·배로 쌓이고 **배가 제일 크고 아래에 온다** — 옆뷰처럼
배를 뒤로 빼면 위에서 내려다본 그림이 되어 걸어가는 방향이 사라진다. 좌우가 대칭이라
`flip`이 뜻을 잃으므로, 걸음은 **왼쪽 세 다리와 오른쪽 세 다리를 번갈아 펴는 것**으로만
읽힌다 (앞뒤 움직임이 안 보이니 좌우 폭이 바뀌어야 한다). 여섯 다리는 가슴 양옆으로
부챗살처럼 편다 — 뒤에서는 앞다리가 몸에 가려 세어지지 않는다. **여기서만 팔을 머리 위로
든다**: 옆뷰가 못 드는 건 앞으로 뾰족하게 나온 입 옆에 팔이 붙어 실루엣이 뭉개져서인데,
등진 개미에게는 그 입이 없다.

**시선만 바꾸는 자세는 머리만 고친다** (`lookUp`·`lookDown`). 몸통·팔·다리를 좌표까지
stand 그대로 두면 고개만 도는 것으로 읽히고, 한 칸이라도 같이 움직이면 시선을 옮긴 게
아니라 화면이 튄 것처럼 보인다. **꾸벅꾸벅 조는 자세도 같은 손이다**
(`doze1`·`doze2`) — 반듯한 머리와 lookDown의 숙인 머리를 갈아끼우면 끄덕이는 게 된다.
**감긴 눈은 새 문자를 안 만든다**: 눈(`e`)을 옆으로 한 칸 늘려 두 칸 나란히 놓으면 그게
곧 "ㅡ"다 (문자를 늘리면 팔레트 키가 따라 는다). **뛰는 자세는 마지막 줄을 비운다** (`jump`) — 발끝이 바닥
줄에 닿아 있으면 아무리 띄워 그려도 서 있는 그림이다. 드는 팔은 **뒤쪽(왼쪽) 팔을 곧게**
세운다: 앞팔을 올리면 튀어나온 입 옆에 붙고, 한 칸짜리 대각선으로 올리면 계단처럼 끊겨
팔로 안 읽힌다 (더듬이와 같은 칸에 몰리면 머리 옆이 점 무더기가 된다). **엎드린 자세는 기어가기의 수평 몸을
세 줄 내려 배를 붙이고**(`prone`) 주둥이 끝을 아래로 꺾는다 — 가로로 두면 엎드린 채 앞을
보는 그림이라 "내려다본다"가 안 남는다. 방향은 **주둥이가 어디를 가리키느냐**가 정한다 —
stand에서 오른쪽으로 곧게 뻗은 주둥이를 위나 아래로 비스듬히 옮기고, 더듬이도 같이
돌린다(젖히면 뒤로 눕고 숙이면 앞으로 선다).

**팔은 머리 위로 안 든다.** 입이 머리 앞으로 뾰족하게 나와 있어서, 팔을 올리면 그 옆에
붙어 실루엣이 뭉갠다. 그래서 파기는 가로 → 비스듬히 아래로만 휘두르고(두 팔을 겹쳐
한 자루처럼 쓴다 — 엇갈리면 "영차"가 된다), 울기는 팔 끝을 6번 줄까지만 올린다.
하트 발사(`shoot`)도 앞으로만 뻗는다 — 뻗는 줄(7)이 입(4번 줄)에서 세 줄 아래라 둘이 안 닿는다.
**날아가는 하트는 문자맵에 없다**: 눈물과 같은 자리로, 시간이 있어야 성립하는 그림은
무대가 손끝 좌표에서 띄운다 (문자맵에 박으면 프레임 수만큼만 날아간다).

**Ants는 개미다.** 초기에 "엔트"를 나무 정령(Ent)으로 오독해 식물로 그린 적이 있다.

### 도트 랩 (자세를 그리는 자리)

`/pixel-lab`([pixel-lab.tsx](apps/web/src/components/pixel-lab/pixel-lab.tsx))은 문자맵을
마우스로 그리고 프레임을 쌓아 움직여보는 **작업용 화면**이다. 손으로 문자열을 세어가며
자세를 뜨던 걸 대신한다 — `"....hhhhehh....."`에서 도트 하나가 어디인지는 눈으로 못 센다.

**만드는 물건은 그림 파일이 아니라 문자맵이다.** 랩이 제 그림 형식을 따로 가지면 옮기는
길에서 도트가 어긋나므로, 화면이 쥐는 것도 내보내는 것도 POSES와 같은 `string[]`이다.
색도 여기서 새로 짓지 않고 `antPalette()`·`antFacePalette()`를 그대로 끌어다 쓴다 —
랩에서 어울리던 색이 앱에서만 튀면 안 된다. 그래서 **단계 슬라이더**가 붙어 있다:
같은 도트를 창백한 개미부터 붉은 개미까지 훑어 실루엣이 어느 단계에서도 안 묻히는지 본다.

읽고 쓰는 창구는 `ant-sprite.tsx`가 낸다 (`antPoseRows` · `ANT_FACE_ROWS` ·
`ANT_COLOR_KEY` · `ANT_FACE_KEY`). **랩이 그림 사본을 들고 있으면 안 된다** — 자세를
고쳤을 때 앱 개미와 랩 개미가 갈린다.

**짤 공장 캐릭터들도 여기서 연다** — 부추·메뚜기·정어리·유인원·햄스터와 국기 여섯,
그리고 32칸으로 다시 뜬 큰 개미. 창구는 `world-cast.ts`의 `CAST_ART`가 내고, 같은 규칙이
그대로 걸린다(사본을 두지 않는다). **팔레트는 캐릭터마다 하나씩**인데, 같은 글자가 그림마다
다른 뜻이라 한 벌로 못 합친다(`w`는 부추에선 알뿌리, 햄스터에선 주둥이다). 캐릭터 팔레트를
칩으로 늘어놓지는 않는다 — 열넷이라 줄만 길어지고, 그림을 열면 그 그림의 팔레트가 딸려 온다.

**화면이 두 층이다 — 서랍(1depth)과 편집창(2depth).** 서랍은 캐릭터를 드롭다운으로 고르고
대표 자세와 거기 딸린 동작들을 한눈에 두는 자리고, 대표를 누르면 편집창으로 내려간다.
**편집창은 자세를 열 때마다 새로 마운트한다** — 안쪽 상태가 전부 "문서 한 벌"을 전제해서
(되돌리기 기록·프레임 번호·고른 글자), 한 창을 재사용하며 자세만 갈아끼우면 앞 자세의
기록이 뒤 자세에 얹힌다. 새로 마운트하면 그 초기화가 공짜고, **window에 걸린 ⌘Z가 서랍에서
몰래 편집창을 되돌리는 사고**도 같이 사라진다.

### 개미 자세 표가 둘이다 — 16칸과 32칸

**앱과 기존 짤이 쓰는 스무 자세(`POSES`)는 안 건드린다.** 도트를 더 잘게 쓰고 싶어서 32칸으로
올려 그려도 16칸 자리에는 안 들어가는데, 한때 그걸 아무도 안 알려줘서 하루치 작업이 갈 곳
없이 남았다. 그래서 32칸은 **표를 따로 둔다**(`BIG_POSES`) — 화면에서도 드롭다운을
`개미 (옛 16칸)` / `개미 (32칸)`로 나눈다. 한 목록에 마흔 개를 섞으면 어느 쪽을 고치는
중인지가 안 보인다. 덮어쓰기 키도 `ant:` / `ant32:`로 갈려 서로를 안 덮는다.

**32칸의 출발점은 앱 서기를 2×2로 늘린 것이다.** 처음부터 다시 그리면 같은 개미가 두 벌이
되므로, 익숙한 그 모양 그대로 칸만 네 배로 잘아진 상태에서 사람이 다듬는다 — 늘린 직후의
그림은 아직 32칸 그림이 아니라 **16칸 그림이 앉아 있는 32칸 격자**다. 색표는 `ANT_BIG`과 같은
것을 쓴다: 잘아진 격자라 눈에 흰자(`e`)와 눈동자(`E`)가 들어가는 자리다.

**기본 자세는 여덟이다** — 서기 · 기어가기1 · 엎드리기 · 등지고 걷기1 · 올려다보기 ·
내려다보기 · 졸기1 · 졸기2. 몸이 서로 달라(세로 몸·가로 몸·뒷모습·머리만 다른 것) 자동
반영으로 못 만드는 것들이라 사람이 하나씩 다듬고, 그래서 서랍에서 **여덟이 전부 대표**로
선다. 팔다리만 다른 파생 자세(손 흔들기·땅파기·울기·뛰기·하트 발사·붙잡기·버둥거리기…)는
이 여덟에서 갈라져 나온다 — **2배로 늘린 팔을 붙이는 게 아니라 32칸 격자에 새로 찍는다.**

`ANT_BIG`(세계의 개미들 짤이 쓰는 32칸 개미)과는 **아직 따로 둔다.** 둘 다 서 있는 옆뷰라
언젠가 한 벌로 합칠 자리지만, 서기를 다 다듬어 놓고 나란히 봐야 어느 쪽으로 합칠지가 정해진다.

**다른 종도 마찬가지로 도트를 쪼갠 판을 따로 둔다.** 부추·유인원·햄스터는 처음부터 32칸이라
할 일이 없고, 아니었던 둘만 늘려 뒀다 — `LOCUST_BIG`(32×20) · `SARDINE_BIG`(44×24). 격자
크기가 32로 안 떨어지는 건 **32이 목표가 아니라 도트를 반으로 쪼개는 게 목표**이기 때문이다.
색표와 이름표는 원본과 **한 벌을 나눠 쓴다** — 두 벌로 두면 색 하나를 고쳤을 때 잘아진 쪽만
옛 색으로 남는다.

**둘 다 아직 어느 판도 안 쓴다.** 정어리는 배율 2로 찍히므로 배율만 1로 낮추면 화면 크기가
그대로지만, **메뚜기는 이미 배율 1이라 갈아끼우면 떼가 두 배로 커진다** — 배율은 정수뿐이라
0.5가 없다. 떼는 작아야 떼로 읽히므로, 쓸 때는 그 크기 문제를 먼저 풀어야 한다.

### 대표를 고치면 딸린 동작이 다시 합성된다

**한 캐릭터의 동작들은 몸통을 좌표까지 공유하고 팔다리만 다르다** — 그래서 대표의 몸을
고치면 딸린 동작들을 **다시 합성해낼 수 있다** (`새 동작 = 대표의 몸통 + 그 동작의 팔다리`).
서기를 고치면 손 흔들기·울기·파기·뛰기·하트 **여덟 개**가 한 번에 따라오고, 엎드리기는
붙잡기를 거느린다.

- **대표의 팔다리는 먼저 벗긴다.** 안 벗기면 서 있는 자세의 팔이 동작마다 새어 들어가
  손 흔드는 개미에게 팔이 하나 더 붙는다.
- **팔다리가 이긴다.** 몸을 넓게 그려 팔 자리를 덮게 되더라도 팔을 남긴다 — 팔이 지워지면
  그건 더 이상 그 동작이 아니다.
- **팔다리로 치는 글자는 캐릭터마다 다르다** (`w`는 개미에선 팔, 부추에선 알뿌리, 햄스터에선
  주둥이다). 글자를 못박지 말고 **팔레트 이름표에서 뽑는다.**
- **묶음은 코드에 적힌 원본으로 판정한다.** 고친 그림으로 재면 대표를 고치는 순간 몸이 달라져
  묶음이 비어버린다 — 반영을 누르기도 전에 대상이 사라지는 셈이다.
- **머리만 다른 자세**(올려다보기·내려다보기·졸기)와 **기어가기·뒷모습**은 몸이 달라 자동
  반영에서 빠진다. 덮으면 시선과 감긴 눈이 사라진다. 화면에 "따로 그린 자세"로 적어 알린다.
- 되돌리기는 **1회**고 이름은 "반영 취소"다 — 편집창의 "되돌리기"(붓질 하나를 무름)와 겹치면 안 된다.

**대표/동작 구조를 가진 건 개미뿐이다.** 나머지 캐릭터는 그림이 하나씩이라 대표만 서고,
아래 목록 자리에는 "아직 동작이 하나야"를 적는다 — 동작이 없는 것과 화면이 고장 난 것은
달라 보여야 한다.

### 고친 그림은 브라우저에 남는다 (규칙 하나를 푼 자리)

**"랩이 사본을 들고 있으면 안 된다"는 규칙을 여기서만 푼다.** 자세를 오가며 고치려면 어딘가
남겨야 하는데 코드에는 아직 안 붙였기 때문이다. **고친 그림만** 덮어쓰기로 쌓고(`yca:pixel-lab:overrides:v1`),
안 고친 그림은 코드에서 그때그때 읽는다. 대신 덮어쓴 그림에는 **"코드에 아직 안 붙음"**을
붙여 원본과 갈라져 있다는 걸 계속 보이게 하고, 원본과 같아지면 덮어쓰기를 지운다.

**브라우저 저장만으로는 두 번 잃었다.** 브라우저·프로필·주소마다 따로고 사이트 데이터를
지우면 통째로 날아간다. 그래서 그릴 때마다 **저장소 안 파일에도 같이 적고**
(`/api/lab-snapshot` → `apps/web/.lab/snapshot.json`), 브라우저가 비어 있으면 거기서 되살린다
— 개발 서버에서만 열리는 통로라 배포에는 없다(404가 오면 그냥 지나간다). **빈 표는 거부한다**:
빈 브라우저가 백업을 덮으면 되살릴 것 자체가 사라진다. 이 파일은 gitignore하지 않는다 —
도트를 찍을 때마다 `git status`가 시끄러워지는 대신 git이 작업을 지켜준다.

일하는 순서는 셋이다. ① 클릭+드래그로 초안을 그리고 **외곽선 두르기**로 덩이 바깥에 한
겹을 두른다 (이미 둘러진 글자는 씨앗으로 안 쳐서 눌러도 선이 자라지 않는다). ② **프레임을
복사**하면 직전 그림이 어니언스킨으로 비쳐, 그 위에서 팔다리만 옮긴다 — 몸통은 좌표까지
그대로 두는 게 자세들의 규칙이다. ③ 재생해 보고 **코드로 뽑아** POSES에 붙인다.

**배포에는 안 실린다** (프로덕션 빌드에서 `notFound()`). 사용자에게 보여줄 물건이 아니라
그림을 만드는 연장이라 앱 표면을 안 넓힌다 — `/meme`은 사용자 기능이라 실리는 것과 다르다.

### 말풍선

개미의 1인칭 혼잣말. **일해야 하는 시간의 크기**로 세기를 정한다 (부정·긍정 각각
`nudgeTier` 7구간) — 반년치면 "여왕개미가 날 버렸어", 몇 분치면 "금방 벌어"
([apps/web/src/components/speech-lines.ts](apps/web/src/components/speech-lines.ts)).
그 시간은 손익을 유저 시급으로 나눈 값이라 곧 "내 노동 몇 시간짜리 손익인가"다.
상단 카운터·하단 한마디와 **같은 시간 축**(`nudgeTier`)을 쓰되 **목소리도 소재도 다르다** —
이쪽은 **개미의 몸과 살림**(다리·더듬이·등껍질, 굴·먹이·여왕개미)으로 말한다. 커피값이나
사표는 하단에 양보한다 (위 표 참고).
**개미 껍질 색과 배경 날씨만 수익률 레벨을 따른다** — 얼굴은 손익률로, 말은 노동시간으로
반응한다. 둘이 어긋날 수 있지만(작은 금액에 −40%면 폭우 속 개미가 "금방 벌어") **부호는
늘 일치**하고 손익률과 시간은 대체로 같이 움직여 거슬리지 않는다 — 그래서 말·상단·하단을
**축 하나(시간)로 통일**했다. %축으로 되돌리지 말 것. **뽑는 시점은 시간 구간이나 손익
방향이 바뀔 때뿐이다** — 매 틱 뽑으면 1초마다 문장이 갈린다. 대문 개미는 손익도 시간도
없어 전용 풀(`HERO_LINES`)에서 뽑는다.

폰트는 픽셀 한글 **Galmuri11**(OFL-1.1)이고, 말풍선 문구 파일들(`speech-lines.ts` ·
`meme/meme-lines.ts`)에 등장하는 글자만 남긴 서브셋을 data URI로 박아 쓴다
(493KB → 17KB). 단일 HTML 목업이 외부 파일을 못 걸기 때문에 인라인이다.
**문구를 고치면 `pnpm --filter @yca/web font`으로 다시 굽는다** —
안 구우면 새 글자만 시스템 폰트로 떨어져 문장 중간에서 서체가 바뀐다.
말풍선을 새로 띄우는 화면이 생기면 그 문구 파일을 `scripts/build-font.mjs`의
`speechSources`에 더할 것 — 빠뜨리면 그 화면에서만 서체가 갈린다.
`apps/web/src/app/galmuri.css`는 생성 파일이니 직접 고치지 말 것.

### 색 규칙

국내 시장 관행을 따른다 — **수익은 빨강(`--up`), 손실은 파랑(`--down`)**.
장대봉·카운터·평가손익 모두 이 규칙을 공유하며, `data-mood` 속성이
`--mood-color`를 바꾸는 방식으로 한 곳에서 결정된다
([globals.css](apps/web/src/app/globals.css)).

### 공유

연봉과 시급은 **절대 공유되지 않는다.** 링크에 실리는 건 종목명·시간·날씨레벨·손익여부뿐이고,
서버에 저장하지 않고 URL 자체에 base64url로 인코딩한다
([packages/shared/src/share.ts](packages/shared/src/share.ts) → `/s/[snapshot]`).
새 필드를 추가할 때 시급·연봉·금액이 새어나가지 않는지 반드시 확인할 것.

**서버에 아무것도 저장하지 않아서 남의 링크를 "찾아낼" 방법은 없다** — 토큰이 곧
데이터라 DB에 남의 것이 존재하지 않고, 아무 토큰이나 지어내봐야 자기가 만든 화면이 뜬다.
대신 **받은 링크 하나는 계산기에 넣을 수 있다.** 종목명이 실리면 그 종목의 종가는
누구나 조회하는 공개 정보이기 때문이다. 그래서 필드는 "보여주고 싶은 것"이 아니라
**"역산돼도 괜찮은 것"**으로 고른다.

- **개미 50단계를 그대로 실으면 평단가가 나온다.** 50단계는 수익률을 2%p 해상도로
  알려주고 `평단 = 종가 ÷ (1 + 수익률)`이라, 링크 하나로 "7만 7천 언저리에 샀다"가
  ±1%로 떨어졌다 (실시간 시세를 붙이면 종가 추정 오차까지 사라져 **더** 정확해진다).
  그래서 링크에는 **배경 날씨 레벨(7)만** 싣고(`v`), 받는 쪽이 `stageFromSceneLevel()`로
  구간 한가운데 단계를 되돌린다 — 역산 정밀도가 ±5%로 흐려진다. 화면이 실제로 쓰는 축이
  어차피 이 7레벨이라(개미 껍질 색은 연속 보간이라 2%p 차이가 눈에 안 보인다) 잃는 게 없다.
  **`encodeShareSnapshotFromStage()`를 쓸 것** — 호출부가 접는 걸 잊지 않도록 만든 문이다.
- **시간(`s`)은 초 단위 그대로 둔다.** 이건 앱이 보여주려는 값 자체라 뭉갤 수 없다.
  대신 남는 건 "손익 ÷ 시급"이고 미지수가 둘이라 **연봉은 복원되지 않는다.**
  다만 연봉을 아는 사람(동료)에게는 손익 금액과 보유 수량이 드러난다 — 알고 두는 선이다.
- **옛 링크(`g`)도 계속 열려야 한다.** 이미 나가 있는 주소가 404가 되면 안 되므로
  `decodeShareSnapshot`이 읽어서 레벨로 접는다. 필드 이름을 `g`에서 `v`로 바꾼 건
  값 범위가 겹쳐서다 — 둘 다 `g`면 `0`이 옛 링크의 대손실인지 새 링크의 본전인지 모른다.
- **종목명(`n`)은 거르지 않고 다듬는다** (`sanitizeName`). 토큰에 서명이 없어 아무나
  지어낼 수 있으므로 24자·안전한 글자로 좁히되, **인코딩에서 던지면 안 된다** — 공유
  버튼을 감싼 async 핸들러가 예외를 삼켜 "눌러도 아무 일 없는 버튼"이 된다. 검증은
  링크를 **읽는 쪽**이 맡는다 (어차피 막고 싶은 건 손으로 만든 토큰이다).

**공유 화면은 검색엔진에 색인되지 않는다** (`robots: { index: false }`). 링크를 블로그나
X에 붙이면 크롤러가 따라오는데, 제목이 "삼성전자 · 출근 131번 만큼 더 벌어야 해.."라
`site:` 검색 한 번으로 남들의 스냅샷을 훑을 수 있게 된다. **robots.txt로는 막지 말 것** —
카카오톡·X의 미리보기 크롤러도 robots.txt를 따르므로 링크 카드가 통째로 안 뜬다.
메타 태그는 페이지를 읽어가는 건 그대로 두고 색인만 막아서 미리보기가 산다.
스토리 PNG도 같은 이유로 `X-Robots-Tag: noindex`를 단다 (이미지 검색 쪽).

**`navigator.share`도 `navigator.clipboard`도 보안 컨텍스트에서만 있다** (HTTPS·localhost).
배포는 HTTPS라 늘 있지만, **실기기 테스트는 LAN IP(`http://192.168.x.x:3000`)로 들어오므로
둘 다 undefined다** — 예전엔 거기서 `navigator.clipboard.writeText`가 TypeError를 냈고,
async 핸들러가 그걸 삼켜 **버튼이 죽은 것처럼** 보였다 ("공유하기가 안 눌린다"). 지금은
[clipboard.ts](apps/web/src/lib/clipboard.ts)가 옛 방식(execCommand)으로 한 번 더 시도하고,
그것도 안 되면 주소를 띄워 손으로 복사하게 둔다. 새 공유 경로를 붙일 때도 **http에서 한 번
눌러볼 것** — 조용히 아무 일도 안 일어나는 게 제일 나쁘다.

**공유 화면은 보낸 사람이 보던 화면 그대로다** ([/s/[snapshot]/page.tsx](apps/web/src/app/s/[snapshot]/page.tsx)) —
카운터, 봉이 자라고 개미가 기어와 말을 거는 **연출까지**, 대시보드와 같은 컴포넌트를 쓴다
(`Countdown` · `CandleScene` · `StageMeter`). 링크용 화면을 따로 그리지 말 것 — 갈라지는
순간 받은 사람은 스크린샷보다 못한 걸 보게 된다. 다른 건 셋뿐이다.

1. **금액이 없다.** 스냅샷에 안 실리니 `Countdown`에 `pnl`을 안 넘기고, 그러면 물건 환산
   줄("맥모닝 1,237개가 그냥 사라졌네..")도 같이 빠진다. 그 줄은 개수 × 물건값으로
   손실액이 되돌려지므로 **공유에 실으면 금액 규칙이 깨진다.**
2. **개미를 눌러도 안 열린다** — 물타기 계산에는 내 평단과 수량이 필요하다.
3. 아래에는 **"다시 계산해보기" 버튼 하나뿐**이다 (종목 카드도, 하단 한마디도 없다).
   받은 사람이 보는 건 남의 손익이라, 할 일은 앱 구경이 아니라 내 평단을 넣어보는 것이다.

### 굽는 이미지 (OG 카드 · 인스타 스토리)

내보내는 그림은 두 판형이다. 둘 다 **화면의 그 무대를 그대로 다시 굽는다** —
밤하늘/땅, 지평선에서 뻗는 장대봉, 그 옆에서 말풍선을 띄운 개미.

| | 크기 | 어디에 | 그리는 곳 |
| --- | --- | --- | --- |
| 링크 카드 | 1200×630 가로 | 카톡·X 링크 미리보기 | [og-card.tsx](apps/web/src/components/og-card.tsx) |
| 인스타 스토리 | 1080×1920 세로 | 공유 시트 → 인스타 | [story-card.tsx](apps/web/src/components/story-card.tsx) |

**무대는 [og-scene.tsx](apps/web/src/components/og-scene.tsx) 하나뿐이고 크기만 받는다.**
판형이 늘어도 무대를 새로 짜지 말 것 — 봉 길이·개미 크기·날씨가 판형마다 따로 놀게 된다.
글자도 [card-text.ts](apps/web/src/lib/card-text.ts) 한 곳에서 나온다 (같은 링크가 두
판형에서 다른 말을 하면 안 된다). 판형이 맡는 건 **배치뿐**이다 — 가로는 글자를 왼쪽에
앉히고 무대를 오른쪽으로 밀며(`candleX`), 세로는 글자 위·무대 가운데로 쌓는다.

카드 전용 그림을 따로 그리지 말 것 — 받은 사람이 링크를 열었을 때 같은 장면을 만나야 한다.
공유 링크(`/s/[snapshot]/opengraph-image`)와 대문(`/opengraph-image`)이 같은 컴포넌트를 쓴다.
**그림은 스냅샷에 실린 값만으로 그려진다** — 그래서 위의 공유 규칙이 여기도 그대로 성립한다.

정지 화면이라 앱과 셋이 다르다. ① 지평선이 손익 방향을 따라 오르내린다(앱은 늘 한가운데).
봉이 뻗는 쪽에 높이를 몰아줘야 한 컷에서 봉이 길어 보인다. ② 개미 최소 크기가 크다 —
타임라인에서 절반으로 줄어든 채 보이므로 그때도 개미로 읽혀야 한다. ③ 날씨는 한 프레임으로
멈춰 그리고 **무대 쪽(오른쪽) 하늘에만** 친다. 왼쪽으로 넘어오면 폭죽 조각이 글자 뒤에 박힌다.
번개는 아예 안 그린다 — 번쩍임은 시간 위에서만 성립하고 한 컷에 담으면 밝은 판때기가 된다.

**satori는 flex만 알고 filter도 애니메이션도 모른다.** 회전 transform도 못 믿으니 폭죽
조각 좌표는 sin/cos로 미리 계산해 절대배치한다. 구름의 blur는 둥근 알약 + 세로
그라데이션으로 흉내 낸다 (radial-gradient는 상자째 칠해져 네모난 구름이 된다).

**한 줄 문구(`SHARE_TAGLINE`)는 [share-copy.ts](apps/web/src/lib/share-copy.ts) 한 곳에 있다** —
카드에 찍히는 글자이자 링크 미리보기의 설명이고 앱 소개 문구다. 세 곳이 같은 문장이어야
그림과 그 옆 텍스트가 따로 놀지 않는다.

카드 문구는 화면과 같은 풀에서 뽑되 **시간에서 나온 씨앗**을 쓴다 — 링크 하나는 늘 같은
카드를 내야 미리보기 캐시가 다시 구워질 때 문장이 갈리지 않는다. **큰 글씨와 말풍선은
씨앗을 어긋나게 준다** — 같은 seed를 두 풀에 그대로 넘기면 늘 같은 자리의 문장끼리
짝지어져 카드마다 두 줄이 같은 소재가 된다 (화면은 seed가 서로 독립이라 가끔이지만
카드는 매번이다). 그래도 같으면 한 칸 밀어 다시 뽑는데, **비교 대상은 말줄(`headline[1]`)**
이다 — `headline[0]`("{시간} 만큼")과 비교하면 검사를 하고도 아무것도 못 거른다.

**폰트는 파일로 들고 있는다** ([src/lib/og-font.ts](apps/web/src/lib/og-font.ts)).
satori에는 시스템 폰트가 없고 화면이 쓰는 woff2는 **못 읽으므로**(ttf/otf/woff만 받는다),
같은 Galmuri11을 woff로 따로 구워 `apps/web/assets/`에 둔다 — 같은
`pnpm --filter @yca/web font`이 CSS와 함께 굽는다. 이쪽은 **현대 한글 전체**(11,172자,
300KB)라 종목명이 뭐가 오든 글자가 안 빠지고, 범위가 고정이라 **문구를 고쳐도 다시 구울
필요가 없다.** 예전엔 구글 폰트에서 매번 받아왔는데 네트워크가 막히면 카드에서 글자가
통째로 빠졌다. 대신 fs로 읽는 파일이라 `next.config.ts`의 `outputFileTracingIncludes`에
`assets/**`가 있어야 배포 번들에 딸려 간다 — 빼면 로컬만 멀쩡하고 배포에서 500이 난다.

**`metadataBase`를 지우지 말 것** ([layout.tsx](apps/web/src/app/layout.tsx)). og:image는
절대 주소로 나가야 크롤러가 그림을 가져가는데, 없으면 Next가 배포마다 바뀌는 주소나
localhost를 채워 넣어 미리보기가 빈 채로 뜬다. 커스텀 도메인을 붙이면 `NEXT_PUBLIC_SITE_URL`로 덮는다.

### 인스타 내보내기

**인스타는 링크 미리보기가 없다.** 링크를 붙여도 그림이 안 뜨고 본문 링크는 눌리지도
않는다. 그래서 인스타로는 링크가 아니라 **이미지를 넘긴다** — 서버가 스토리 규격
PNG를 굽고([/s/[snapshot]/story](apps/web/src/app/s/[snapshot]/story/route.tsx)),
앱이 그걸 받아 공유 시트에 **파일로** 얹는다
([story-export.tsx](apps/web/src/components/story-export.tsx)). 시트에서 인스타그램을
고르면 스토리 편집기로 바로 들어간다. 링크 카드와 달리 크롤러가 아니라 사람이 받아
가므로 파일 규칙(`opengraph-image`)이 아니라 라우트 핸들러다.

**누르면 바로 공유하지 않고 미리보기 시트를 먼저 띄운다.** iOS는 `navigator.share()`를
사용자 제스처 안에서 불러야 하는데 이미지를 받아오는 `await`가 그 제스처를 소모한다 —
미리 받아두고 시트 안에서 다시 누르면 새 제스처라 통과한다. 겸사겸사 뭘 올리는지 보고
가게 된다.

**이미지에는 링크를 심을 수 없다.** PNG는 픽셀뿐이고, 스토리에서 누를 수 있는 건 올리는
사람이 붙이는 **링크 스티커**뿐이다 (그것도 인스타 앱 안에서만 붙는다). 그래서 카드에는
날 주소를 안 적고 **서비스명만** 남기고, 대신 공유하는 순간 앱이 **공유 링크를 클립보드에
넣어준다** — 스티커에 붙여넣기 한 번이면 눌러서 들어올 수 있는 스토리가 된다. 복사는
`await` 없이 시작해야 한다: 기다리면 iOS가 사용자 제스처를 잃어 바로 뒤의 `share()`가
거부당한다.

위아래 여백이 큰 것도 규격 때문이다 — 인스타 UI가 위(프로필 줄)와 아래(답장 입력창)를
덮으므로 **여백을 줄이면 글자가 그 밑으로 들어간다.**

**시트 제목은 "걱정마, 다른 정보는 절대 공유되지 않아!"다** — 이 버튼 앞에서 사람이 먼저
품는 의문이 "누르면 내 연봉도 같이 나가나?"라서, 무엇을 하는지보다 무엇을 안 하는지를
먼저 답한다. 시트에서 글자를 쥔 버튼은 **"이미지 친구에게 보내기" 하나뿐**이고, 닫기와
저장은 아이콘만 남긴다 — 여기서 읽을 것은 제목과 이미지지 버튼 이름이 아니다.

**브라우저 사정을 화면에 적지 말 것.** 공유 시트가 없는 환경(데스크톱·http)에서는
보내기가 조용히 저장으로 떨어진다 — 예전엔 "지금 주소(http)에서는 브라우저가 이미지
공유를 막아" 같은 안내를 띄웠는데, 쓰는 사람 잘못도 아니고 그 자리에서 할 수 있는
일(이미지를 손에 넣는 것)은 어차피 같다. 사정 설명 대신 되는 일을 하면 된다.

목업에는 이미지를 구울 서버가 없어 `@/lib/story-image`가 null을 돌려주고 **버튼째
사라진다** (아래 "단일 HTML 목업"의 alias 참고).

### 짤 공장 (움직이는 짤 · 영상)

`/meme`([meme-studio.tsx](apps/web/src/components/meme/meme-studio.tsx))는 개미가 나오는
9:16 짤들을 돌려보고 **영상이나 이미지로 내보내는** 화면이다. 대시보드 아래 "움직이는 개미 짤
만들기"로 들어간다.

**탭이 둘이다 — 움직이는 짤 / 한 컷 이미지.** 영상은 무겁고 못 받는 자리가 있어서(카톡
프로필, 커뮤니티 글, 캡처 한 장이면 되는 곳) 이미지 한 장이 더 멀리 간다. **판을 두 벌로
나누지 않는 게 규칙이다**: 같은 장면 함수를 흐르게 두느냐 한 순간에 멈춰 세우느냐의
차이뿐이라, 새 판을 더하면 양쪽에 함께 생긴다. 모드는 **판보다 먼저 묻는다** — 영상이냐
한 장이냐에 따라 고를 판이 달라지고 버튼 문구도 거기서 갈린다.

한 컷 모드에는 **판마다 "제일 그 판다운 순간"이 정해져 있다** (`MemeScene.stillMs`).
0으로 두면 대개 아무 일도 안 일어난 첫 프레임이라, 이미지 탭을 연 사람이 제일 먼저 보는
그림이 빈 무대가 된다 — 곡괭이가 흙을 튀기는 찰나, 봉이 천장을 뚫은 자리처럼 **말풍선까지
또렷하게 떠 있는 순간**으로 잡는다 (알파가 0.99 아래면 반투명한 말풍선이 그대로 박힌다).
순간은 슬라이더로 옮길 수 있고, 그건 고르는 사람 몫이다.

**한 컷 모드에서는 시간을 안 흘린다.** rAF를 돌려놓고 마지막 프레임만 저장하면 화면에서
본 그림과 저장된 그림이 한 프레임씩 어긋난다 — 고른 순간이 곧 결과물이라야 슬라이더를
미는 게 의미가 있다. 이미지도 **바로 안 내려받고 시트를 먼저 띄운다** (영상과 같은 이유 —
`toBlob`을 기다리는 사이 iOS가 사용자 제스처를 잃는다). 시트는 영상과 **한 벌을 같이 쓴다**:
하는 일이 같아서 두 벌로 나누면 한쪽만 고쳐지는 자리가 된다.

**이 화면은 연봉도 평단도 시세도 안 읽는다.** 그래서 온보딩을 안 거친 사람도 열 수 있고,
공유 규칙(금액을 화면 밖으로 안 내보낸다)이 여기서는 아예 성립할 일이 없다 — 나가는 건
개미 그림과 말풍선뿐이다. **장면에 숫자를 넣지 말 것**: 손익이나 수익률을 그리기 시작하면
이 화면도 재무 화면이 되고, 그 순간 "영상으로 내보내기"가 금액을 내보내는 버튼이 된다.

| 판 | 무슨 일이 | 한 바퀴 |
| --- | --- | --- |
| 땅파기 | 해가 쨍쨍한 흙벽 앞에서 판다. 땀이 튀고, 굴에서 동전이 튀어나와 쌓인다 | 4.8초 |
| 차트 탑승 | 차트 계단 끝 봉에 올라탄 개미로 카메라가 붙고, 배경이 산 → 구름 → 우주로 넘어간다 | 7.2초 |
| 눈물바다 | 울면 눈물이 수면이 되어 차오른다. 잃은 파란 봉이 떠다닌다 | 7.2초 |
| 클로즈업 | 우는 얼굴만 화면 가득. 두 눈에서 눈물이 줄줄 흐른다 | 7.2초 |
| 돈방석 | 동전 더미에 앉아 양옆으로 장대양봉이 오른다. 동전이 쏟아지고 반짝인다 | 4.8초 |
| 롤러코스터 | 개미 셋이 탄 수레가 오르내린다. 레일을 받치는 기둥이 양봉·음봉이다 | 7.2초 |
| 만원 열차 | 정류장에 열차가 서고 개미들이 우르르 탄다. 출발해 봉 라인을 달린다 | 12초 |
| 빈 지갑 | 지갑을 닫았다 폈다 한다. 있었는데요, 없었습니다 | 9.6초 |
| 불기둥 | 흰 차트 창. 아침 장에 불기둥이 솟고(폭죽), 뒤집혀 창 밖으로 내려간다(비) | 9.6초 |
| 물타기 | 물뿌리개로 동전·지폐를 부으면 양봉이 조금씩 자란다. 마지막엔 새싹으로 클로즈업 | 10.6초 |
| 코인 상자 | 상자 셋을 박아 미니 캔들을 키우고, 전쟁·금리 깃발 괴물을 하나씩 걸어오게 해 한 방에 밟아 죽이고, 두 번째 상자에서 나온 지폐를 먹고 커져 바닥 상자 위로 뛰면 불기둥이 솟아 그걸 타고 화면 밖으로 사라진다 | 17.2초 |
| 남극 탐험 | 등지고 달리는 개미. 양봉인 줄 알고 다가가면 음봉으로 뒤집혀 얼음이 깨진다 | 14.4초 |
| 계단과 엘리베이터 | 계단은 한 칸씩 네 번, 엘리베이터는 네 층을 1.8초에. 그 격언 그대로 | 9.6초 |
| 세계의 개미들 | 나라마다 개인투자자를 부르는 이름이 다르다. 개미(한국) → 부추(중국) → 메뚜기(일본) → 정어리(브라질) → 유인원(미국) → 햄스터(러시아)가 차례로 나와 제 몸짓을 한다 | 30초 |
| 일희일비 | "일희일비 금지" 표어가 걸린 스크린 앞. 9시에 장이 열리고 캔들 서른 개가 찍히는 동안 개미가 춤추고 울고, 횡보가 이어지면 졸고, 급등엔 개미 둘이 튀어나와 같이 춤춘다 | 31.2초 |

#### 그림은 저해상도 격자에 그리고 늘린다

무대는 **108×192(9:16) 격자**에 그린 뒤 `imageSmoothingEnabled = false`로 10배 늘려
1080×1920으로 찍는다 ([pixel-canvas.ts](apps/web/src/lib/pixel-canvas.ts)). 격자 한 칸이
곧 도트 하나라, 좌표를 정수로만 쓰면 도트 크기가 화면 어디서나 같다. **처음부터
1080×1920에 그리지 말 것** — 선 하나 그을 때마다 10으로 나눠야 하고, 반올림이 어긋나는
순간 도트가 들쭉날쭉해져 개미 스프라이트와 다른 해상도의 그림처럼 보인다.

예외는 **말풍선 글자**뿐이다. 픽셀 폰트는 11px 격자라 무대 격자(10px)에 얹으면 한 글자가
110px가 된다. 글자는 늘린 뒤 원본 해상도에 따로 그리되 **크기를 11의 배수로만** 잡는다
(55px = 도트 5px). 배수가 아니면 폰트 도트가 정수 픽셀에 안 떨어져 흐려진다
([meme-bubble.ts](apps/web/src/components/meme/meme-bubble.ts)).

#### 장면은 시간을 받아 그리기만 한다

`draw(p, { time, seed })` — 장면 함수는 상태도 타이머도 안 쥔다
([meme-scenes.ts](apps/web/src/components/meme/meme-scenes.ts)). 시간은 rAF 한 곳에서만
흐르고, 무작위가 필요한 곳(별자리·흙 얼룩·튀는 흙)은 `seededRandom(seed)`을 쓴다.
**`Math.random()`을 그리는 중에 부르면 안 된다** — 1초에 60번 자리가 바뀌어 배경이
지글거리고, 미리보기에서 본 화면과 녹화된 영상이 달라진다. "다시 뽑기"는 씨앗만 바꾼다.

배경이 고도를 따라 넘어가는 판(탑승)은 **하늘색을 화면 위치가 아니라 세계 좌표로** 정한다.
그래야 올라가는 내내 색이 이어지고, 산이 사라지는 높이와 별이 뜨는 높이가 늘 같은 곳에
있다. 정거장 배열(`SKY_STOPS`)은 오름차순·내림차순을 둘 다 받는다 — 예전엔 오름차순만
받아서, 세계를 위에서 아래로 적은 배열이 조용히 **첫 색 하나로 고정**됐다(하늘이 아예 안
변했다). 타입 검사가 못 잡는 종류의 사고라 `gradientAt` 쪽에서 막는다.

**개미가 배경에 묻히지 않게 색을 고른다.** 흙벽 앞 개미는 붉은 단계(45)를 쓰고 벽은
바닥보다 어둡게 칠하며 모서리에 볕 한 줄을 세운다 — 흙색 개미를 흙벽에 세웠더니 실루엣이
통째로 사라진 적이 있다. 밤에 우는 개미(창백한 2단계)는 반대로 **지평선 쪽 하늘을 밝혀**
띄운다. 배경을 못 바꾸는 자리에서는 개미 쪽에 선을 두른다 — 같은 그림을 상하좌우로
**한 도트(=scale)씩** 밀어 어둡게 깔거나(`outlined()`, 금빛 후광 위의 돈방석 개미),
한 도트 밀어 밝게 깔아 위·오른쪽 모서리만 남긴다(클로즈업 얼굴의 파란 림라이트).
**1픽셀씩 밀지 말 것** — 개미 도트보다 가는 선이 생겨 해상도가 어긋나 보인다.

#### 루프는 정수 번 도는 것으로만 만든다

영상은 딱 한 바퀴라 **마지막 프레임 다음이 첫 프레임**이다. `Math.sin(t * 3)`처럼 흐른
초를 그대로 넣으면 한 바퀴 끝에서 값이 뚝 끊겨, SNS에서 반복 재생될 때마다 이음매가
보인다. 그래서 흔들리는 것(별 반짝임·전구·아지랑이·물결·구름)은 전부 `oscillator(a)`를
거쳐 **한 바퀴에 정수 번** 돈다. 두 프레임을 갈아끼우는 `flip2(time, ms)`도 주기가 루프를
**짝수로** 나눠야 하고(홀수면 첫 프레임과 마지막 자세가 다르다), 파티클 주기도 루프의
약수여야 한다.

**되돌아오지 않는 움직임은 예외다.** 봉이 자라고(돈방석), 물이 차오르고(눈물바다),
고도가 올라가는(탑승) 건 장식이 아니라 이야기라 한 바퀴가 끝나면 처음으로 돌아가는 게
맞다 — 그건 흠이 아니라 컷이다. 고칠 것은 **깜빡임처럼 아무 때나 일어나도 되는 것이
하필 이음매에 걸린 경우**다 (클로즈업의 눈 깜빡임을 반 박자 밀어둔 이유).

#### 클로즈업은 도트를 키우는 게 아니라 쪼갠다

얼굴만 크게 잡는 판은 16×16 몸을 확대하지 않는다 — 눈이 한 칸이라 아무리 키워도 표정이
안 생긴다. 대신 **52×80으로 따로 그린 문자맵**(`FACE`)을 쓴다. 화면에 찍히는 크기는
같고(48칸 × 2배 = 96칸) 그 안의 도트만 잘아져서, 같은 자리에 눈꺼풀·흰자·눈동자·반짝임·
입술선이 다 들어간다. 껍질 색은 몸과 **같은 계산**(`antTones`)에서 갈라져 나온다 — 색을 두
곳에서 따로 만들면 클로즈업 컷만 서서히 다른 벌레가 된다. 눈 감은 프레임은 얼굴을 한 벌 더
두지 않고 **눈이 있는 줄만 갈아끼운다**(`FACE_BLINK`): 두 벌을 손으로 맞춰두면 한쪽만
고쳤을 때 깜빡일 때마다 얼굴형이 미세하게 튄다.

**이 얼굴만 3/4 측면이다.** 몸은 옆뷰지만 얼굴은 살짝 돌린 각도로 그린다 — 우는 표정의
핵심은 큼직한 두 눈이라 완전한 옆얼굴로는 눈이 하나뿐이라 성립하지 않고, **완전한 정면도
안 된다**: 본보기 그림(우는 개구리 밈)의 두툼한 입술 띠는 3/4 각도라서 주둥이를 감싸는
것으로 읽히는데, 5:5 정면에서 같은 띠를 그으면 감쌀 주둥이가 없어 얼굴을 가로지르는
줄무늬가 된다. 실제로 정면으로 그렸다가 "저 주름은 뭐냐"는 말을 들었다.

각도는 **비대칭 넷**으로 만든다. ① 앞으로 나온 볼을 한쪽에만 덧붙여 머리 윤곽을
기울이고, ② 먼 쪽 눈은 작게·가까운 쪽 눈은 크게 잡고, ③ 주둥이를 곧은 띠가 아니라
**휘어 올라가는 띠**로 쓸어 그리고, ④ **입꼬리를 한쪽에만 남긴다** — 반대쪽 꼬리는 볼에
감겨 얼굴 윤곽 뒤로 넘어간다. 양끝을 똑같이 좁히면 입이 아니라 얼굴에 두른 허리띠가 된다.
넷 중 하나만 빠져도 다시 정면으로 보인다.

**입술은 볼 라인 밖으로 나온다.** 얼굴 윤곽 안에서 잘리게 두면 입술의 제 외곽선이 얼굴선에
먹혀 사라져, 튀어나온 주둥이가 아니라 얼굴에 그린 무늬가 된다. 그래서 주둥이는 배경 위에도
칠하고 마지막 윤곽 처리에 함께 넣어 제 선을 갖게 한다 — 대신 머리를 그만큼 안쪽으로 밀어
튀어나올 자리를 비워둔다. 위아래 입술 두께는 같게(선을 띠 한가운데) 둔다.

정면이든 측면이든 개미로 읽히는 건 표식을 그대로 두기 때문이다 — 위로 뻗은 더듬이 두 개
(각도를 따라 길이가 다르다)와 같은 껍질 색. 눈은 머리 윤곽 위로 튀어나오고, 눈꺼풀은
절반쯤 덮는 두꺼운 활이다.

**개미 단계는 읽히는 쪽으로 고른다.** 이 판은 손익을 안 그리므로 단계가 뜻하는 건 색뿐인데,
창백한 쪽(2~8)으로 두면 어두운 배경에 얼굴이 통째로 묻힌다. 탈진한 개미라는 설정보다
얼굴이 보이는 쪽이 먼저다.

**가로선을 나란히 두지 말 것.** 입 아래에 턱 그늘의 윗변, 그 아래에 얼굴과 몸을 가르는
선이 겹치면서 셋이 나란히 누워 주름으로 보였다. 턱 그늘은 어느 줄부터 자르지 말고 머리
아래 곡선을 따라 초승달로 깐다.

**눈물은 문자맵에 없다.** 줄줄 흐르는 건 시간이 있어야 하는 그림이라 무대가 그린다 —
두 눈 아래(`ANT_FACE_EYES`)에서 화면 끝까지 줄기를 잇고, 그 안에서 밝은 마디가 내려간다.
**줄기는 얇게 둔다**: 얼굴 폭만큼 굵게 그렸더니 표정이 물에 덮여 안 보였다.

#### 표정을 새로 만들 때 (놀란 얼굴)

빈 지갑 판은 얼굴이 또 하나 필요했다(`FACE_SHOCK`). **머리·각도·몸은 우는 얼굴과 똑같이
두고 눈·눈썹·입만 바꾼다** — 그래야 같은 개미가 표정만 바꾼 것으로 읽힌다. 놀란 눈은
흰자를 넓게 두고 **눈동자를 작게** 찍어 만든다(크게 찍으면 그냥 뜬 눈이다). 눈동자는
지갑 쪽인 아래를 보고, 눈썹은 안쪽을 치켜올리고, 입은 다문 띠가 아니라 벌어진 구멍이다.

얼굴 맵은 몸까지 한 장이지만 이 판은 **머리까지만 잘라 쓴다** — 몸까지 그리면 얼굴이
화면을 다 먹어 지갑이 턱 밑에 눌린다. 잘라낸 아래는 무대가 어깨로 받쳐준다.

**지갑은 위아래로 편다.** 좌우로 펴면 화면을 가로지르는 띠가 되고, 무엇보다 개미가
정면으로 서서 두 손을 벌린 자세가 된다 — 사진처럼 **몸을 살짝 틀어** 한 손은 위, 한 손은
아래를 잡아야 "펼쳐 보이는" 그림이 나온다. 그 자세를 넣을 자리를 만들려고 개미를 한 배로
줄였다(두 배로 그리면 얼굴이 화면을 다 먹는다). 덕분에 얼굴 도트가 무대 도트와 같은
크기가 되어 지갑과 한 해상도로 붙는다.

몸통을 그릴 때 두 번 헛디뎠다. **윤곽 원을 몸 위에 덮어 그리면** 몸이 통째로 검은
덩어리가 된다 (윤곽을 먼저 깔고 몸을 얹는다). **팔을 몸통과 같은 색으로 칠하면** 통째로
묻힌다 (밝게 칠하고 위아래로 선을 둘러 떼어낸다). 몸통은 원이 아니라 타원이라야 턱까지
닿으면서도 어깨 폭이 산다.

**펴진 지갑은 속이 주인공이다.** 처음엔 가죽을 안쪽까지 덮어 그렸는데, 활짝 편 지갑인데도
갈색 판때기 두 장으로 보여서 "비었다"가 안 읽혔다. 지금은 가죽을 테두리로 돌리고 속을
어둡게 비운다 — 카드 칸만 남기고.

#### 봉은 한 자리에서 뒤집는다 (불기둥)

무대가 **흰 차트 창**인 판이다. 다른 판이 무대(흙·물·하늘)를 그리는 것과 달리 화면 자체가
시세 창이라, 폭죽도 비도 창 테두리 안에서만 친다(`p.clipped`). 창을 화면 가장자리에서 떼어
앉히는 것도 규칙이다 — **여백이 곧 "이건 차트 창이다"라는 표시**고, 그만큼 봉과 개미가 작아져
한 화면에 이야기가 다 들어온다. **날씨는 앱과 같은 규칙이다**: 오르면 폭죽, 내리면 비.

**기준선이 곧 시가다.** 봉은 늘 이 선에서 시작해 위(양봉)나 아래(음봉)로 뻗으므로, 기둥이
창을 오르내리는 게 아니라 **한 자리에서 색이 뒤집히며 방향만 바뀐다** — 차트에서 실제로
일어나는 일이 그렇고, 개미가 같은 자리를 올려봤다 내려보는 그림도 여기서 나온다.

**개미가 뭘 하는지는 시간이 아니라 봉이 정한다.** 천장을 뚫을 즈음 뛰고(`jump`), 봉이 눈높이
위면 올려다보고, 기준선 아래로 내려가면(=음봉) 엎드려 내려다본다(`prone`). 막 경계에 걸었더니
봉은 아직 하늘에 있는데 개미가 먼저 고개를 숙였다. 꼬리(고가)도 같다 — 천장을 상수로 쓰면
아직 자라는 중인 봉 위로 닿지도 않은 데까지 꼬리가 미리 서 있다.

**마지막엔 몸통을 시가에서 떼어 창 밖으로 내려보낸다.** 몸통 위쪽이 기준선에 못 박혀 있는 한,
아무리 내려가도 화면에는 파란 판때기 하나가 그대로 서 있다 — 사라지는 게 보여야 "야 너
어디가"가 성립한다. **꼬리는 위 끝만 안 움직이고 아래로 늘어난다**: 고가에 박힌 채 몸통
꼭대기까지 따라 내려와 붙어 있고, 색은 몸통을 따라 파랗게 바뀐다. 위 끝까지 같이 내리면
기둥이 아니라 차트가 통째로 미끄러지고, 기준선에서 끊으면 기둥과 꼬리가 두 동강 나 남남이
된다 — 둘 다 한 번씩 그려보고 나온 자리다. 대신 **남는 것은 한 바퀴 끝에서 지운다** —
창을 세로로 가로지른 꼬리는 마지막에 흐려지고, **두께가 한 줄도 안 되는 봉은 아예 안 그린다**(최소 두께를 두면 봉이 없는 순간에도
막대가 한 줄 남는다). 그래야 한 바퀴 끝이 빈 차트와 서 있는 개미뿐이라 다음 바퀴 첫
프레임과 같은 그림이 된다.

말은 뽑지 않고 **대본 여섯 컷**이다 (`PILLAR_SCRIPT`, 아침 장 → 장중 → 종가). 하루가 흐르는
판이라 순서가 곧 이야기고, 뽑기에 맡기면 종가 얘기가 첫 컷에 뜬다. 컷 길이(루프 ÷ 6)가 막
경계와 맞물려 있으니 **막을 옮기면 대본 줄 수도 같이 본다.**

#### 말장난이 그림이 되는 판 (물타기)

물을 준다 = 물을 탄다. **물뿌리개에서 나오는 건 물이 아니라 동전과 지폐**고, 한 번 부을
때마다 양봉이 조금씩 자라며 뿌리는 땅속 KOSPI 이름표까지 내려가 있다. 이런 판에서는
그림 쪽에서 더 보탤 게 없다 — 농담이 이미 그림이라 **숫자를 얹지 않는다** (지수 이름만
이름표로 박고, 얼마를 부었는지는 짤 밖의 일로 둔다).

**자란 키는 한 바퀴 끝에서 되돌아간다.** 돈방석의 동전 더미·눈물바다의 수면과 같은
자리로, 되돌아오지 않는 움직임이라 이음매가 컷이 된다 — 그게 이 판의 농담이기도 하다
(네 번을 부어도 다음 날 아침이면 도로 그만큼이다). 대신 **한 번에 자라는 키를 작게 잡아**
컷이 툭 튀지 않게 한다.

**물줄기는 촘촘해야 물줄기다.** 서너 개를 띄엄띄엄 날리면 물이 아니라 동전을 던지는
그림이 된다 — 여덟 개를 촘촘히 흘려보내고, **마지막 방울이 닿을 때까지 물뿌리개를 기울인
채 둔다** (먼저 세우면 허공에서 돈이 쏟아진다). 떨어지는 자리도 봉 위가 아니라 **밑동**이고,
동전과 지폐는 **다른 판보다 크게** 그린다 — 잘게 그리면 그냥 빗방울이라 "물=돈"이 안 읽힌다.

**작아지는 건 물건이지 그림이 아니다.** 개미를 한 배 작게(스케일 2) 그리고 봉·뿌리·이름표를
그에 맞춰 줄여 가운데에 모으되, **하늘과 땅은 화면을 끝까지 채운다** — 배경까지 같이 줄여
액자에 넣었더니 짤이 아니라 액자 사진이 됐다. 여백은 배경 위의 빈자리로 둔다. 그리고
**도트를 0.9배로 줄이는 길은 없다** — 격자 한 칸이 도트 하나라 스프라이트 배율은 정수뿐이다
(3 → 2). 나머지는 좌표로 줄인다.

**줄어들면 안 되는 것도 있다.** 이름표 글자(KOSPI)는 뿌리가 어디에 닿아 있는지를 말하는
글자라 작고 흐리면 있으나 마나다 — 그림이 작아진 만큼 **굵게** 찍는다. 굵게는 브라우저의
bold가 아니라 **도트 하나만큼 옆으로 겹쳐 찍기**다 (`SceneLabel.bold`): 픽셀 폰트에는 굵은
자체가 없어 합성 볼드가 획을 문질러 흐려진다.

**마지막 막은 새싹으로 밀고 들어간다.** 네 번을 부어놓고 화면이 그대로 끝나면 "그래서
얼마나 컸는데"가 안 보인다 — 카메라가 붙어야 그 조그만 새싹이 결론이 된다. 클로즈업은
**같은 그림을 배율(`k`)만 올려 그린다**: 클로즈업용 그림을 따로 두면 같은 새싹이 두 벌이
되어 서서히 갈라진다. 대신 **새싹은 원·선이 아니라 문자맵**이다 — 원으로 그리면 3배로
키웠을 때 도트가 아니라 매끈한 도형이 커져서 가까이 갈수록 해상도만 높아진 그림이 된다.
흔들림도 소수가 아니라 **도트 단위(-1·0·+1)로 끊는다.**

**개미는 같이 커지지 않는다.** 스프라이트 배율은 정수뿐이라 2 → 6으로 뛰면 개미만 갑자기
세 배가 된다. 카메라가 붙는 동안 옆으로 밀려나며 흐려지게 두는 게 낫다 — 이 컷의 주인공은
새싹이다. 말풍선도 이때 **개미 머리에서 새싹 위로 옮겨간다** (컷이 바뀌며 새로 뜨는 것이라
자리가 뛰는 게 안 보인다). 말하는 이가 새싹이라 마지막 줄만 "니 월급"이다.

클로즈업에서는 **한 박자 더 머문다** — 카메라가 붙자마자 끝나면 새싹을 볼 새가 없다.
한 바퀴 끝이 클로즈업이라 **이음매는 컷이다** — 되감기가 아니라 다음 컷으로 넘어가는
것으로 읽힌다 (자란 키가 되돌아가는 것과 같은 자리다).

도트 그림은 회전을 못 하므로 **기울인 물뿌리개는 따로 그린다** (열차 칸을 안 기울인 것과
같은 이유). 흙빛 위의 이름표도 규칙이 같다 — **판때기는 도트로, 글자는 늘린 뒤에 얹는다**
(`labels`). `drawLabels`는 늘 검은 글자라, 밝은 판때기가 없으면 흙 위에서 안 읽힌다.

#### 옆으로 흐르는 무대 (코인 상자)

**개미는 제자리고 세계가 흐른다.** 화면에 못 박힌 개미 앞으로 배경을 왼쪽으로 밀면
걸어가는 그림이 된다 — 개미를 오른쪽으로 옮기면 화면 끝에서 할 일이 없어진다. 가로는
끝까지 이 규칙 하나다 — 뒤에 괴물을 밟고 불기둥을 타는 막이 늘어도 개미는 **한 번도
가로로 옮겨지지 않는다**, 세로(높이)와 배율만 막마다 달라진다.

**상자를 박기까지는 세계 한 마디(`BLK_WORLD`)에 걸음을 맞춘다.** 예전엔 이 판 전체가
걸어 들어오고(스크롤) · 서서 박고(정지) · 걸어 나가는(스크롤) 루프라 한 바퀴 끝에 첫
프레임과 같은 세계 자리로 되돌아왔다. **지금은 상자를 다 박고 나면 이야기가 한 방향으로
흐른다** — 괴물 둘을 차례로 밟아 죽이고, 두 번째 상자에서 나온 지폐를 먹어 커지고,
몇 걸음 더 걸어 바닥 상자를 밟아 불기둥을 띄워 타고 화면 밖으로 사라진다. 되돌아오지
않는 여정이라 물타기의 자라는 봉과 같은 예외를 받는다: 한 바퀴 끝은 "빈 화면"이 아니라
"개미가 막 사라진 하늘"이고, 다음 바퀴는 다시 걸어 들어오는 첫 장면부터다 (이음매는
컷이다). 걷는 속도(`BLK_SPEED`)는 상자를 몇 개 박든 고정값이다 — 예전 이 판이 걸어
들어오고 나가는 걸음이 같은 세계 눈금 위에 있었을 때의 속도를 그대로 물려받았다.
구름은 **절반 속도**로 흘리되(원경) 구름 무늬 마디도 절반으로 잡아야 그 앞 구간의
이음매가 맞는다.

**한 번만 등장하는 소품은 배경과 같은 접기(`blockAt`)를 안 거친다.** 언덕(캔들 스카이라인)·
토관·벽돌·상자1(셋)은 `BLK_WORLD`마다 되풀이돼야 하니 월드 좌표를 모듈로 접어 화면에
올리지만, 괴물 둘·상자2·지폐·상자3·불기둥은 딱 한 번 나왔다 사라지는 이야기 소품이라
**월드 좌표에서 스크롤을 그냥 뺀 값**을 쓴다 — 되풀이될 일이 없는 걸 굳이 접으면
언젠가 스크롤이 한 바퀴(216)를 넘을 때 엉뚱한 자리로 순간이동한다.

**초록 언덕도 이 판의 농담에서 못 빠진다.** 배경 언덕 세 무더기를 초록 계단 대신
**양봉·음봉을 섞은 미니 캔들 스카이라인**(`drawCandleHill`)으로 바꿨다 — "차트 위의
마리오"라는 설정이 지형에도 스며든다. 높이는 고정 배열이라 다시 뽑아도 지형이 안
바뀐다 (다른 배경 소품과 같은 대우).

**상자는 하나가 아니라 셋, 걸어가며 하나씩 박는다.** 세 상자가 한자리에 나란히 서
있고 개미가 그 앞을 지나가는 게 아니라 — **상자를 박고(`BLK_HIT_MS`) → 다음 상자로
한 걸음 걷고(`BOX1_STEP_MS`) → 또 박는다**를 세 번 반복한다. 개미는 여전히 가로로
안 움직이므로, "걷는다"는 건 스크롤이 한 걸음만큼 더 흐르는 것이다 — 그래서 상자마다
세계 좌표를 따로 정의하지 않고, **몇 걸음째냐**(`BLK_BOX1_STOP[i]`)에서 화면 좌표를
바로 뽑아낸다(`BLK_BOX1_STOP[i] + BLK_BOX_OFFSET - scroll`). 매번 같은 간격(38칸
앞)에 멈춰 서므로 **점프는 늘 지금 상자의 바로 밑에서** 일어난다 — 제자리에서 3연타를
하면 뒤쪽 상자 둘은 개미 머리 위가 아닌 곳에 떠 있는 것으로 보이던 문제가 이래서
사라진다.

박을 때마다 코인 대신 **상자에서 미니 캔들이 자란다** — 음봉(손실) → 더 긴 음봉 →
마지막엔 방향이 뒤집힌 작은 양봉. 폭은 다른 캔들(6칸)의 1.5배(9칸)다 — 상자에서 막
튀어나온 캔들이라 눈에 띄게 굵어야 한다. 캔들은 **현재 어느 상자를 박고 있느냐와
무관하게, 제 시각이 지났으면 계속 자라 있는다**(`for (let i = 0; i < BLK_HITS; i +=
1)` 루프가 절대 시각으로 각자 판단한다) — 다음 상자로 걸어가는 동안에도 방금 박은
캔들이 화면에 남고, 상자와 같은 좌표 공식을 쓰므로 걸어서 멀어지면 배경처럼 함께
화면 밖으로 흘러간다. 말도 순서가 정해져 있다("똥 밟았네 → 뭐야 이거 → 도파민이
부족해") — 실망이 쌓이다 마지막에 엉뚱한 데서 신이 나는 손실 중독의 감각이라 뽑기에
맡기지 않는다(`BLOCK_TRIPLE_SCRIPT`). **말은 캔들이 자란 뒤에 뜬다**(히트 시작
600ms 뒤 — 캔들이 자라기 시작하는 420ms보다 늦다) — 차트를 먼저 보여주고 그다음
개미가 반응해야 무엇에 대한 말인지 읽힌다. 순서를 반대로 하면 "뭐야 이거"가 아직
안 나온 캔들에 대한 말이 된다.

**괴물은 꼬리 없는 말풍선 모양이다** (`drawFlagMonster`). 깃발을 따로 안 들고 **깃발
글자를 몸통 안에 그대로 앉힌다** — `SceneLabel`을 몸 가운데 겹쳐 얹으면 몹 자체가
살아 움직이는 팻말이 된다. 글자는 크게 찍는다(unit 6) — 작으면 몸통 안에서 묻힌다.
위에는 눈 둘만 붙는다(입도 팔다리도 없다) — 다가오는 말풍선 두 개라는 그림 하나로
적대감은 충분하다. 각진 사각형은 모서리를 한 칸씩 하늘색으로 되짚어 지워 **말풍선처럼
둥글어 보이게** 한다.

**괴물은 둘, 각자 한 방에 죽는다.** "전쟁" 괴물이 먼저, 죽으면 "금리" 괴물이 이어서
등장한다 — 순서가 곧 농담이라(전쟁이 오고 그다음 금리 인상이 온다) 뽑기에 맡기지 않고
`drawEnemyEncounter` 하나를 두 번 부른다(둘이 완전히 같은 요령이라 함수로 묶었다 —
두 벌로 손으로 맞춰두면 한쪽만 고쳐질 자리가 된다). **한 번 뛰어올라 착지하는 순간
`squish`가 바로 1로 튄다** — 중간 단계 없이 즉시 찌부러지고 낮고 넓어지며 눈이
사라져야 "한 방"으로 읽힌다. 착지 후 남는 시간을 그대로 죽는 연출(입자가 튀며
사라짐)에 쓰고, 밟는 순간 개미는 고정된 한마디를 한다 — 전쟁 괴물에게 "꺼져!",
금리 괴물에게 "에잇!"(`BLOCK_STOMP_TAUNTS`), 둘 다 뽑지 않고 고정이다.

**갑자기 나타나면 안 되므로 화면 밖에서부터 걸어 들어오게 한다.** 정지한 채로 배경
스크롤에 실려 오면 저 혼자 움직이는 게 아니라 세계가 미는 것처럼 보인다 — 그래서 걷는
동안 몸을 사인파로 살짝 씰룩이는 것(`bob`)까지 더해야 "이동"으로 읽힌다.

**지폐는 코인이 아니다.** 이 판에서 코인(비트·이더)을 완전히 걷어내고 상자에서 나오는
물건을 **전부 지폐로 통일**했다 — 물타기 판의 작은 지폐보다 세 배 크게 그린다
(`drawSeedBill`). 뜨는 순간 "시드 등장!"이라는 말풍선이 붙는데, **꼬리는 개미가
아니라 지폐를 향한다** — 이 한 줄만은 개미가 아니라 지폐 자신이 하는 말이다. 노출
시간도 다른 말풍선의 두 배(약 520ms)로 늘려서, 뜨는 지폐를 따라 자리도 계속 옮겨준다
(정지된 자리에 남으면 지폐는 이미 저만치 갔는데 말만 남아 있는 것으로 보인다). 상자에서
튀어오른 뒤 개미까지 포물선을 그리며 걸어오고, 닿는 순간 사라지며 반짝인다.

**상자2는 지폐를 다 먹으면(`BLK_SEED_END`) 함께 치운다.** 렌더 범위를
`BLK_E2_STOMP_END`부터 열어두기만 하고 안 닫으면, 다 쓴 상자가 레벨업·다음 걸음
내내 화면 오른쪽 위에 둥둥 떠 있는 채로 남는다 — 실제로 그렇게 남던 버그였다.
지폐를 다 먹은 뒤에는 그 상자를 다시 쓸 일이 없으니 범위를 `BLK_SEED_END` 앞에서
닫고, 마지막 150ms만 살짝 페이드아웃해 사라지는 게 급작스럽지 않게 한다.

**LEVEL UP은 개미 말풍선과 다른 손이다.** 말풍선은 개미의 1인칭 혼잣말이고, "LEVEL
UP!"은 화면 가운데 뜨는 게임 UI 글자라 `SceneLabel`로 큼직하게 얹는다 — 두 글자가 같은
자리에서 같은 말을 하면 굳이 둘 다 띄운 보람이 없다. 몸이 커지는 동안 발은 바닥에 붙여
두고(`antFeetY`는 그대로, 배율만 자란다) 위로 자라는 것으로 읽히게 한다.

**불기둥은 상자를 밟아야 나온다 — 레벨업 직후에 바로 안 뜬다.** 커진 개미가 몇 걸음
더 걸어(`BLK_WALK3_MS`) **바닥에 놓인 세 번째 물음표 상자**를 만나고, 그 위로 뛰어오르는
순간(`BLK_JUMP3_MS`) 착지 스파크와 함께 상자에서 불기둥이 솟는다 — 몸이 커진 것과
날아오르는 것 사이에 "뭘 밟아서" 하나가 더 있어야 인과가 읽힌다. 상자는 다른 둘과
달리 바닥(`BLK_GROUND`)에 붙어 있다 — 공중에 뜬 물음표 상자가 아니라 밟는 스위치처럼
쓰기 위해서다.

**불기둥의 몸통은 다른 봉과 똑같이 곧게 그린다** — 굴곡 없는 사각 막대 + 가운데
하이라이트 줄뿐이다. 이 앱의 모든 봉이 이 모양이라(장대봉·캔들 스카이라인·다른 짤의
차트 봉) 몸통을 흔들면 이 기둥만 딴 재질처럼 보인다. **가로 폭은 다른 캔들의 두 배**다 —
상자를 뚫고 솟아오르는 결정적인 한 방이라 다른 캔들보다 굵어야 힘이 실린다. 불꽃은
몸통을 일그러뜨리지 않고 **옆에서 널름거리는 조각**으로만 더한다. **화면 밖(음수 y)까지
그린다** — 개미가 오르는 내내 꼭대기가 남아 있어야 하므로, 짧게 끊으면 오르는 도중에
지붕이 먼저 사라져 보인다. 개미는 옆에 붙어 크롤 자세를 빠르게 갈아끼우며 세로로만
올라가고(가로 자리는 여전히 고정), 위로 갈수록 가속해(t²) 마지막엔 화면 밖으로 빠르게
빨려 나가는 것으로 여정이 끝난다.

#### 차트를 눕혀 원근으로 깐다 (남극 탐험)

도트 펭귄이 남극을 달리던 그 화면이다. **개미는 등을 보이고, 움직이는 건 세계다** —
앞으로 나아가는 건 눈밭이 다가오는 것으로 그리고, 개미가 하는 일은 좌우로 트는 것뿐이다.
화면에서 개미의 자리는 x만 조종되고 y는 못박혀 있다. 다른 판이 개미를 옆에서 보는 것과
달리 여기는 뒤에서 보므로 **뒷모습 자세**를 쓴다 (위 "개미 50단계").

**값이 곧 깊이다.** 봉 하나는 카메라에서 멀어지는 쪽으로 누운 판때기고, 몸통은 두툼하게
꼬리는 가늘게 더 멀리 뻗는다 — 세워 그리면 눈밭 위에 간판이 서 있는 그림이 되어 개미가
그 위로 달려갈 자리가 없다. 눕힌 것은 전부 `polarBand` 하나로 그린다: **깊이 하나로 크기와
좌우가 같이 정해지므로**(줄마다 배율을 다시 잰다) 봉이든 구멍이든 눈밭 결이든 원근이
어긋날 자리가 없다. 내려앉는 깊이도 화면 도트가 아니라 **개미 깊이에서의 도트 수**로 주고
줄마다 환산한다 — 똑같이 내리면 원근을 무시한 판때기가 통째로 미끄러진다.

**눈밭 밖까지 회색으로 덮지 말 것.** 얼음 둔덕은 눈밭 가장자리에 쌓인 **띠**다. 처음엔
바깥을 통째로 칠했는데 화면 절반이 회색이 되어 남극 벌판이 아니라 산등성이 사이 골짜기로
보였다. 그래도 회색은 있어야 한다 — 온통 흰 화면에서는 원근이 안 보여 달려도 제자리처럼
보인다. 결·둔덕 층은 **깊이 눈금마다 하나씩 놓고 통째로 흘려보낸다**: 간격이 한 판에 흐르는
거리의 약수라 이음매에서 어긋나지 않는다.

**꼬리는 남고 몸통만 내려앉는다.** 다 와서 뒤집힐 때 몸통이 꼭대기부터 빠지며 윗꼬리만
남고, 그 자리에서 파란 음봉이 개미 발밑까지 뻗는다 — 뻗어온 음봉이 얼음을 깨고 아래로
꺼져도 꼬리는 안 깨진 눈밭에 그대로 붙어 있어야 "꼬리를 남기며 뒤집혔다"가 화면에 남는다.
**꼬리 색은 몸통이 뻗는 시간을 같이 쓴다** — 한 프레임에 같이 갈아끼우면 화면이 깜빡인 것처럼 보인다.

**물에 빠진 개미에만 테두리를 두른다.** 파란 물 위로 나온 갈색 몸이 물빛에 묻혀 머리인지
떠다니는 얼음 조각인지 구분이 안 갔다. 흰 눈밭 위에서는 두르면 오히려 개미가 검게 뭉친다.
물보라도 파랑이 아니라 **흰 거품**이라야 보이고, 개미 몸 밖으로 빼야 실루엣을 안 가린다.

**빠져 있는 동안 하늘에 우는 얼굴이 비친다.** 클로즈업 판이 쓰는 그 얼굴 맵을 그대로
가져다 옅게 깐다 — 얼굴을 여기서 새로 그리면 같은 개미가 두 벌이 된다. 개미는 물에 빠져
허우적대는 중이라 정작 표정이 안 보이는데, 그 표정을 배경이 대신 짓는다.

셋을 지켜야 얼굴이 배경으로 남는다.

1. **하늘 안에서만 그린다** (앱의 날씨와 같은 규칙 — 땅에 비가 내리면 이상하다). 눈밭까지
   걸치면 배경에 비친 표정이 아니라 화면 위에 얹은 그림이 되고, 발밑에서 벌어지는 일을 가린다.
   그래서 배율도 한 배다: 두 배로 키웠더니 얼굴이 화면을 세로로 다 덮었다.
2. **머리까지만 쓴다** (빈 지갑 판과 같은 자리에서 자른다). 목 아래 가슴이 따라오면
   하늘이 통째로 막힌다.
3. **산보다 뒤에 그린다.** 턱을 산이 가려줘야 저 멀리 떠 있는 얼굴로 보이고, 얼굴을
   통째로(70%) 진하게 깔아도 하늘이 안 막힌다. 한때 속을 비우고 윤곽선만 남겨봤는데,
   배경은 안 죽어도 얼굴이 아니라 도면으로 보였다 — 가리는 건 투명도가 아니라 산이 한다.

처음엔 이 자리에 **하늘 자막**(고독하다 …)을 띄웠는데, 글자는 상황을 설명할 뿐이라 발밑에서
이미 벌어지는 일을 한 번 더 말하는 셈이었다. 얼굴은 설명하지 않고 표정만 남긴다.

**빠지는 순간에는 개미 옆에 하락률이 뜬다** (-10% → -20% → -30%, 판마다 깊어진다).
**이 숫자는 대본이지 계산값이 아니다** — 짤 공장은 연봉도 평단도 시세도 안 읽으므로 여기
%는 유저의 수익률이 아니라 못박아둔 글자다. 손익에서 온 숫자를 그리기 시작하면 그 순간
"영상으로 내보내기"가 금액을 내보내는 버튼이 된다.

글자는 **개미의 화면 안쪽 옆, 말풍선 꼬리 아래**에 세운다 — 개미가 판마다 좌우로 트므로
바깥쪽에 두면 한 판에서는 화면 밖으로 잘리고, 머리 옆에 붙이면 꼬리 끝과 겹쳐 말풍선
그림자에 묻힌다 (말풍선은 머리 위, 하락률은 그 아래다). 개미와 같이 가라앉으므로 따로
떠오르게 하지 않는다. 파란 물 위에 파란 글씨라 **흰 외곽선**을 두르는데, 선은
**도트 하나(=`unit`)만큼 밀어** 여덟 방향으로 깐다 — 1픽셀씩 밀면 글자 도트보다 가는 선이
생겨 해상도가 어긋나 보인다 (개미에 테두리를 두를 때와 같은 규칙). 색·외곽선은 `SceneLabel`이
받고, **무대 글자가 쓰는 크기는 `LABEL_UNITS`에 적어 미리 깔아둔다** — 안 깔면 첫 프레임에서
그 글자만 시스템 폰트로 떴다가 뒤늦게 바뀐다.

**세 번 당하는 게 한 바퀴다.** 세 판이 기계적으로 똑같아서 이음매가 저절로 맞는다 — 한 판이
끝나면 세계는 정확히 한 판 거리만큼 흘러 있고 개미는 얼음 위를 달리는 중이다 (**구멍에 빠져
있는 동안은 세계가 선다** — 개미가 못 달리니 흐를 이유가 없다). 판마다 다른 건 어느 쪽
양봉으로 트느냐와 말풍선뿐이라, 같은 일을 세 번 당하는 게 그대로 보인다.

말은 뽑지 않고 **대본 여섯 컷**이다 (찾음 → 속음 × 3). 짝마다 기대는 세지고 반응은 짧아지다
마지막엔 **모자이크**로 가려진다 — 뭐라고 했는지 적지 않는 쪽이 무슨 욕을 골라 적든 그것보다
세다. **세 번째 "지금이니!"만 늦게 뜬다**: 앞의 두 번은 양봉을 발견하자마자 떠들지만 이번엔
붉은 봉에 올라서기 직전까지 말이 없다가 발을 딛는 순간 입을 연다. 일찍 띄우면 앞 두 판과
박자가 같아져, 마지막만 세지는 게 사라진다.

#### 격언을 그대로 그린다 (계단과 엘리베이터)

"주가는 계단으로 오르고 엘리베이터로 내려온다." **농담이 이미 완성돼 있어서 보탤 게 없다** —
이 판이 할 일은 두 속도를 **눈으로 재게** 하는 것뿐이다. 네 칸을 한 칸씩 4초에 올라 한 층을
얻고, 승강기는 네 층을 1.8초에 내려간다. 그림 쪽에서 더 웃기려 들면 격언이 안 보인다
(물타기와 같은 자리 — 말장난이 그림인 판에는 숫자도 장식도 안 얹는다).

**계단은 한 층 걸러 하나씩만 놓는다.** 층마다 놓았더니 계단 덩어리가 제 층계참을 통째로
덮어, 다 올라간 개미가 다음 계단 속에 파묻혔다 — 단면 그림에서 계단은 바닥까지 찬 덩어리라
그 층을 지나다닐 수 없다. 계단 없는 층이 층계참이 되고, **승강기 문도 거기에만 있다.**

**한 바퀴는 정확히 네 층 아래에서 끝난다.** 한 층 올라(계단) 네 층 내려가고(승강기) 한 층
더 내려오면(계단) 다시 계단 밑이고, 그동안 카메라도 네 층 흘러 있다 — 계단 주기(두 층)의
배수라 이음매가 저절로 맞는다. **층 이름은 안 적는다**: 같은 층이 무한히 되풀이되는
건물이라 번호를 붙이면 매 바퀴 거짓말을 한다. 지하로 내려갔다는 건 말풍선이 맡는다
(남극 판의 하락률과 반대 경우다 — 거기서는 못박은 숫자가 대본이었다).

**카메라는 승강기에만 붙는다.** 칸과 똑같은 거리를 내려가므로 내려가는 내내 칸은 화면에
붙박여 있고 건물만 위로 쏟아진다 — 승강기에서 밖을 보는 그 그림이다. 계단을 오르내릴
때는 반대로 카메라를 세워두고 개미가 화면 위아래로 움직인다. 둘 다 카메라가 따라가면
개미가 늘 화면 한가운데 붙어 있어 **올라간 건지 내려간 건지가 안 남는다.**

**문은 유리다.** 불투명하게 닫으면 제일 중요한 1.8초 동안 개미가 화면에서 사라진다.
그리고 **개미를 유리 위에 그린다** — 유리를 개미 위에 덮었더니 갈색 몸이 허옇게 떠서 자세가
안 남았다. 문짝 가장자리 선만 개미 위로 지나가게 두면 또렷하면서도 문 안에 선 것으로 읽힌다.
개미가 서는 자리도 칸 한가운데가 아니다 — 두 짝이 만나는 이음매 선이 몸을 세로로 가른다.

**난간 하나가 붉은 덩어리를 계단으로 만든다.** 발판만 그렸을 때는 층마다 빨간 블록이 쌓인
그림이었는데, 비스듬한 선이 지나가자 오르는 길로 보였다. **속도선은 여기서만 씨앗을 시간으로
흔든다** — 다른 판은 매 프레임 자리가 바뀌면 배경이 지글거려서 씨앗을 고정하지만, 속도선은
떨려야 흐르는 것으로 보인다 (대신 90ms에 한 번씩만 다시 뽑아 잡음이 되지 않게 한다).

#### 캐릭터가 로테이션되는 판 (세계의 개미들)

**이름이 곧 그 나라 개인투자자가 당하는 방식이다** — 부추(중국)는 잘려도 또 자라고,
메뚜기(일본)는 떼로 몰리고, 정어리(브라질)는 고래가 무섭고, 유인원(미국)은 뭉치고,
햄스터(러시아)는 볼에 쟁인다. 그래서 몸짓도 말도 이름에서 나온다. **개미가 맨 앞이다** —
이 앱의 주인공이 기준점이 돼야 나머지가 "다른 나라 개미"로 읽힌다.

**무대는 하나로 공유한다.** 배경까지 컷마다 새로 지으면 여섯 판을 만든 셈이고, 로테이션이
아니라 슬라이드쇼로 보인다 — 컷이 더하는 건 소품뿐이다(가위·좌우 양봉·고래 그림자·바닥
봉). 정어리 컷만 물빛으로 물들이는데, 고래가 무섭다는 말이 물속이라야 서기 때문이다.

**컷마다 캐릭터가 들어왔다 나가므로 한 바퀴 끝도 시작도 빈 무대다** — 이음매가 저절로 맞는
드문 판이다. 각 컷의 `draw`는 **컷 안의 시간만** 받는다: 전역 시간을 넘기면 컷마다 위상이
달라져 들어오는 연출이 매번 다른 자리에서 시작한다.

**개미 아닌 몸은 [world-cast.ts](apps/web/src/components/meme/world-cast.ts)에 모은다.**
`meme-scenes.ts`가 이미 6,500줄이라서고, **개미만은 여기 안 온다** — `antPixels()`를 그대로
쓴다(몸을 딴 파일에서 다시 그리면 앱 개미와 짤 개미가 갈린다는 규칙 그대로다).

그리는 방식은 **새싹(`SPROUT`)을 따른다**: 문자맵 + 그림마다의 색 키(char → hex) + 정수
배율. 개미 색 키가 팔레트 이름을 거치는 것과 다른데, 개미는 단계마다 껍질 색이 변하지만 이
캐릭터들은 안 변하기 때문이다. **`p.sprite`는 안 쓴다** — 뒤집기가 `15 - x`로 개미 격자
폭(16)을 못박고 있어서 16칸이 아닌 그림을 뒤집으면 엉뚱한 축으로 뒤집힌다. 그래서
`drawRows()` 하나가 이 파일 그림 전부를 찍고 뒤집기도 직접 맡는다.

**몸은 32칸 격자에 배율 2로 찍는다** (16칸 × 3이 아니다). 화면에 찍히는 크기는 비슷하고
**그 안의 도트만 잘아져서**, 눈·팔·잎처럼 한두 칸으로는 못 그리던 게 들어간다 — 클로즈업
얼굴이 도트를 키우는 대신 쪼개는 것과 같은 손이다. **개미만 16칸 몸이라 배율 4로 키를
맞추고, 그래서 개미 컷만 도트가 굵다** — 앱 개미와 같은 몸을 쓰는 값이다. 메뚜기는 떼라서
배율 1이다(몸 도트가 곧 무대 도트다).

**눈은 흰자(`e`)와 눈동자(`E`) 두 칸으로 그린다** — 캐릭터 전부 같은 규칙이다. 한 칸짜리
검은 점은 눈이 아니라 얼굴에 뚫린 구멍으로 보인다: 흰자가 있어야 어디를 보는지가 생기고,
그래야 캐릭터로 읽힌다. **개미도 이 판에서는 32칸 몸(`ANT_BIG_ROWS`)을 쓴다** — 16칸 몸을
배율 4로 키우면 도트만 굵어져 다른 캐릭터들 옆에서 혼자 뭉툭하고, 눈도 한 점이 전부다.
클로즈업 얼굴을 따로 두는 것과 같은 자리라 **`ant-sprite.tsx` 안에 두고 색도 같은
`antPalette()`에서 갈라진다** — 딴 파일에 두면 앱 개미와 갈린다.

**유인원 팔은 문자맵에 없다.** 들었다 내렸다 하는데 몸에 붙여두면 자세마다 몸을 한 벌씩 더
그려야 한다 — 개미가 "프레임 사이에 바뀌는 건 팔뿐"인 것과 같은 자리라 팔만 따로 두고
어깨에 붙였다 머리 옆으로 옮긴다. **햄스터 팔은 몸보다 밝게 칠하고 어두운 선을 두른다** —
몸통 색 그대로 두면 통째로 묻혀 팔이 있는지도 모른다 (빈 지갑 판에서 한 번 헛디딘 자리다).

**계속 변하는 크기는 문자맵에 안 넣는다** — 부추가 잘린 길이는 `grown`으로 위에서부터 줄을
빼고, 햄스터 볼은 무대가 몸 뒤에 덧그린다. 프레임을 몇 벌 두면 그만큼만 부풀고 중간 크기가
안 나온다 (눈물·하트와 같은 자리). 둘 다 **도트 단위로 끊는다.**

**이름이 위아래로 갈린다.** 국기 **위**에 나라 이름, 화면 **아래 땅**에 그 나라가
개인투자자를 부르는 이름(개미·부추·메뚜기…)이 붙는다 — 위가 "어느 나라"라면 아래는
"거기선 뭐라 부르나"라서, 둘을 한 줄로 붙이면 농담이 사라진다. 땅 이름은 **판때기를 깔고
그 위에 얹는다**: `drawLabels`가 주는 먹색 글자는 흙빛 위에서 안 읽힌다.

**국기는 15×10 도트고 색은 한 벌로 접었다** — 일장기의 진홍과 오성홍기의 주홍은 이 크기에서
안 갈린다. **성조기와 태극기는 단순화가 불가피하다**(줄 열셋과 별 쉰, 태극과 괘 넷이 15칸에
안 들어간다) — "그 국기인 줄 알아볼 정도"로만 남기고 옆의 나라 이름이 확인해 준다. 흰 바탕
국기가 밝은 하늘에 묻히므로 **테두리를 한 줄 두른다.**

#### 표어가 개미를 반박한다 (일희일비)

스크린 위에 **"일희일비 금지"**가 걸려 있고 개미는 캔들 하나하나에 춤추고 운다 — **표어와
행동이 어긋나는 게 이 판의 농담 전부라** 여기서도 숫자를 안 얹는다 (물타기·계단과 같은 자리).
개미가 표어를 지키는 유일한 구간이 **조는 동안**인 것도 그래서다: 의지가 아니라 졸음이라 웃긴다.

**한 바퀴가 하루다.** 빈 차트와 시계(8:59 → 09:00 → 장시작!)로 열고, 캔들 스무 개가
차례로 찍힌다. 차트가 차오르는 건 되돌아오지 않는 움직임이라 **한 바퀴 끝이 컷이다**
(물타기의 자라는 봉과 같은 예외). 대신 **마지막 캔들을 횡보로 못박아** 이음매에서 개미가
`stand`로 서 있게 했다 — 춤추다 첫 프레임으로 끊기면 그건 컷이 아니라 튄 것으로 보인다.

**첫 봉은 화면 한가운데 서고, 새 봉이 오른쪽으로 붙는다.** 제일 새 봉이 오른쪽 한계에
닿으면 **세계가 왼쪽으로 흘러** 지나간 봉을 창 밖으로 밀어낸다 — 실시간 차트가 하는 그
움직임이다. 스무 개를 창 폭에 미리 나눠 앉히면 **다 그려진 차트를 켜놓은 그림**이라 시간이
안 흐른다. 흐름은 봉 단위로 툭툭 끊지 않고 **봉이 자라는 동안 이어서** 민다 (칸씩 뛰면
차트가 아니라 슬라이드가 된다). **세로 눈금도 같이 흘려야** 한다 — 눈금이 가만히 있으면
차트가 흐르는 게 아니라 봉만 미끄러지는 것으로 보인다. 오른쪽 한계는 창 끝에서 한 뼘
떼어둔다: 끝에 딱 붙이면 다음 봉이 자랄 자리가 없어 매번 잘린 채로 태어난다.

**표어는 주식창 위쪽에 걸쳐 앉는다.** 창 위 여백에 얹으면 화면 제목처럼 보이지만, 창을
물고 있으면 **사무실 벽 팻말이 모니터 앞을 가린** 그림이 된다 — 표어와 차트가 한 덩이로
읽혀야 "저걸 걸어놓고도 저런다"가 산다. 그래서 **캔들보다 나중에 그린다** (먼저 그리면
창이 팻말을 덮는다). 창에 겹친 아래쪽에만 그림자를 한 줄 깔고, 더 아래로 내리지는 않는다 —
캔들 꼬리가 올라오는 자리와 겹치면 팻말이 차트를 먹는다.

**개미는 통째로 스크린 앞에 선다.** 발끝이 단상 윗면(=스크린 아래끝)에 닿게 앉혀 몸 전체가
밝은 창을 등지게 한다 — 아래로 내리면 **다리가 어두운 단상에 묻혀** 개미가 단상 뒤에 선
것처럼 보인다 (실제로 그렇게 그렸다가 다리가 통째로 사라졌다). 말풍선이 차트를 안 가리도록
캔들 밴드는 그만큼 위로 물린다.

**대목의 봉은 두 배로 오래 머문다** (장 시작 두 컷 · 횡보 · 급등). 장이 안 움직이는 구간은
실제로도 길게 느껴지고, 조는 연출은 한 박자로는 읽히지도 않으며, 급등은 개미 둘이 튀어나와
춤출 시간이 필요하다 — 봉 수를 늘려 그 시간을 버는 대신 **봉 하나를 오래 두면** 평범한
구간의 봉 수를 안 깎아도 된다. 대신 캔들 길이가 제각각이 되므로 **몇 번째 캔들인지를
나눗셈으로 못 구한다**: 시작 시각을 미리 깔아두고(`BRD_STARTS`) 훑는다. 길이를 정하는
판정과 조는 판정이 어긋나면 느린 구간과 조는 구간이 따로 노는데, **무작위 봉은 최소 등락이
4%라 횡보가 될 수 없어서** 못박은 자리만 보면 둘이 늘 일치한다.

**급등은 장대양봉 셋이다.** 무작위 봉의 최대(13%)보다 확실히 굵어야 셋이 잇달아 선 게
급등으로 읽힌다. 이때 **좌우 화면 밖에서 개미 둘이 미끄러져 들어와 같이 춤추고 물러난다** —
가운데를 보도록 오른쪽 개미만 뒤집고, 셋의 자세를 반 박자씩 어긋나게 갈아끼운다 (같은
프레임을 쓰면 셋이 한 몸처럼 움직여 인형처럼 보인다). **주인공보다 먼저 그린다**: 겹치는
자리에서 주인공이 앞에 서야 누가 이 판의 개미인지가 안 흔들린다. 조는 구간 바로 뒤에
붙여둔 것도 의도다 — 졸다가 급등에 깨서 다 같이 뛰는 게 이 판의 한 장면이다.

**감긴 눈은 개미 도트보다 얇게 긋는다.** 문자맵 한 칸이 곧 도트 셋(스프라이트 배율)이라
그대로 두면 감긴 눈이 뜬 눈만큼 두꺼워 그냥 눈으로 보인다 — 무대가 위아래 한 도트씩을 껍질
색으로 덮어 가운데 한 줄만 남긴다. 개미 도트보다 가는 선은 원래 안 만드는 규칙이지만
(테두리·외곽선), **여기서는 그 가늚이 곧 "감겼다"는 표시다.**

**차트는 스무 개를 한 번에 다 뽑아두고 시간이 흐르는 만큼만 그린다.** 그려진 만큼만 재서
세로 배율을 잡으면 차트가 차오를 때마다 앞 캔들이 위아래로 튄다. **걸음은 곱셈이 아니라
덧셈이다** — `level *= (1 + pct)`로 쌓으면 스무 걸음 뒤 값이 몇 배로 벌어져 낮은 쪽 캔들이
한 줄도 안 되게 눌리고, `rect`가 양끝을 반올림하므로 **아예 안 그려진다.** 같은 이유로
몸통은 한 줄을 보장한다 (횡보 구간이 빈칸이 되면 안 된다).

**못박는 건 셋뿐이다** — 조는 연출이 매번 나오도록 가운데 횡보 여섯 칸, 한 컷 이미지가
멈춰 설 큰 음봉 하나, 이음매를 위한 마지막 횡보. 나머지는 무작위인데 **최소 등락이 4%라
자연 발생 횡보가 없다** — 그래서 조는 구간은 늘 못박은 자리 하나뿐이고, 큰 음봉이 그
구간을 잇거나 쪼갤 일이 없다. 조는 구간은 그리는 쪽에서 뒤로 거슬러 세지 않고 **차트를
뽑을 때 미리 세어둔다** (거슬러 세면 0·1번 캔들과 장 시작 전에서 경계가 어긋난다).

**`seededRandom`의 첫 draw는 버린다.** 짤 공장이 주는 씨앗은 1~997이라 첫 값이
0.00002~0.022에 몰려 있다 — 그대로 쓰면 "다시 뽑기"를 눌러도 0번 캔들은 늘 같은 방향에
같은 크기다. 같은 이유로 캔들마다 `seededRandom(seed + i)`를 새로 만들지 않는다 (이웃
씨앗의 첫 값 차이가 2e-5라 스무 개가 전부 같아진다) — 생성기 하나로 끝까지 뽑는다.

**말풍선은 박자가 아니라 캔들 사건에 걸린다** (코인 상자가 상자를 박을 때 말하는 것과 같은
손). 큰 변동과 조는 구간에만 뜨고 **한마디가 캔들 둘만큼 떠 있다** — 캔들 하나(0.66초) 안에서
뜨고 지면 `FADE_MS` 때문에 알파가 0.73까지밖에 안 올라, **한 컷 이미지에 반투명한 말풍선이
그대로 박힌다.** 말과 말 사이에는 숨을 두는데, 안 두면 조는 동안 말풍선이 쉬지 않고 이어
붙는다. **몸짓·순서에 매인 말만 뽑지 않고 못박는다** — 장 시작 두 컷("오늘 시작부터 왜이래 ?"
→ "오늘 점심 굶어야 하나?")은 순서가 곧 이야기고, 하트를 날리는 순간("유-후 하트")과 눈이
감기는 순간("z..Z.z 졸리네")은 말이 그림과 같이 나와야 한다. 풀에 맡기면 그 몸짓이 나온
판에서 엉뚱한 말이 뜨고 정작 이 말은 몇 판에 한 번만 나온다. 이런 말은 **반드시 나와야 하니
앞말이 아직 떠 있어도 밀고 들어간다** (`BRD_MUST_SAY`) — 대신 그 몸짓이 나올 봉의 자리도
같이 못박아 둔다.

#### 말풍선이 장면 안에서 갈릴 수 있다

보통은 장면 하나에 풀 하나지만, **만원 열차는 봉이 오를 때와 내릴 때 하는 말이 다르다**
(`trainUp` / `trainDown`). 그래서 문구 풀의 이름(`MemeLinePool`)은 장면 이름보다 넓고,
장면이 지금 지나는 봉의 방향을 보고 풀을 갈아 끼운다 — 이 판은 그게 전부라, 박자
(`BEAT_MS`)로 끊으면 봉이 바뀌는 순간과 어긋난다.

**카메라는 일정한 속도로 간다.** 출발 가속을 넣었더니 뒤쪽 봉들이 순식간에 지나가
말풍선이 뜨다 말았다. 봉마다 한마디씩 하는 판은 구간에 같은 시간을 줘야 한다.
막이 여럿인 판(도착·탑승·출발·주행)은 `speakWindow`로 **막의 창 안에서** 말한다 —
전역 박자로 끊으면 막이 바뀌는 순간 말풍선이 중간에 잘린다.

#### 말풍선은 접고, 개미 머리 위를 비운다

긴 문구는 **글자를 줄이지 않고 줄을 바꾼다** ([meme-bubble.ts](apps/web/src/components/meme/meme-bubble.ts)).
예전엔 화면 폭에 들어가는 제일 큰 크기를 골랐는데, 그러면 긴 줄만 글씨가 작아져 판을 넘길
때마다 말풍선 크기가 널뛰었다. 크기를 낮추는 건 **한 낱말이 통째로 폭을 넘길 때뿐**이다
(그건 접을 자리가 없다).

접는 폭은 **화면 폭의 3분의 2**다. 여백까지 꽉 채워 접으면 한 줄짜리 긴 띠가 되어 말풍선이
아니라 자막처럼 보인다. 그리고 **줄은 고르게 나눈다** — 앞에서부터 채우기만 하면 마지막
줄에 한 낱말만 떨어지므로("눈 뜨면 올라 있을 / 거다"), 접고 난 뒤 **줄 수가 늘지 않는
선에서 제일 좁은 폭**을 찾아 한 번 더 접는다. 한 문구는 늘 같은 결과라 캐시해 둔다
(1초에 60번 재는 자리다).

문구에 **줄바꿈(`\n`)이 박혀 있으면 거기서 먼저 끊는다** — 접히는 모양이 농담의 일부인
줄이 있어서다 ("아직 니 월급이 / 더 필요해"). 나머지 접기는 그 조각 안에서 한다.

꼬리는 **계단 여섯 칸**이라 상자가 개미 머리에서 그만큼 떠 있는다 — 짧게 두면 말풍선이
머리에 얹힌 것처럼 보인다. 문구가 14자로 묶여 있는 건 여전한데(`check-lines.mjs`), 이제
그 상한은 "한 줄"이 아니라 **두 줄 안쪽**을 뜻하고, 재는 것도 문구 전체가 아니라 **줄
하나**다 (줄바꿈이 박힌 문구는 조각마다 따로 접히므로 통째로 세면 접힐 걸 미리 거른다).

**이모지는 도트로 직접 그린다** (`ICONS`). 픽셀 폰트에 없는 글자라 시스템 폰트에 맡겼더니
캔버스가 그 자리를 **빈칸으로 그려버렸고**, 컬러 이모지로 떨어지는 환경에서도 도트 글씨
옆에서 혼자 매끈해 튄다. 그림글자는 **11×11 도트**라 폰트와 같은 격자를 쓰고, 한글 한 자와
폭이 정확히 같아 재는 것도 접는 것도 글자와 똑같이 다뤄진다.

문구 파일에는 **이모지 글자를 그대로 적는다** (`"...귀여워 ❤️"`) — 읽는 사람에게 그게 제일
잘 읽히고 글자 수 검사도 한 글자로 세어준다. 바꿔 끼우는 건 그리는 쪽뿐이다. 새 그림글자가
필요하면 `ICONS`에 11×11 문자맵을 더한다 (물타기는 하트·돈·새싹 셋).

#### 무대에 글자를 박을 때 (역 이름 간판)

말풍선 말고도 화면에 글자가 필요한 자리가 있다 (역 이름). `SceneBubble.labels`로 넘기면
말풍선과 같은 픽셀 폰트로 그려지는데, **판때기는 무대가 도트로 그리고 글자만 얹는다** —
상자까지 글자 쪽에서 그리면 도트 그림과 다른 해상도의 네모가 무대 한가운데 서게 된다.

역 이름(`STATION_NAME`)은 상수 하나다. 여기만 고치면 다른 종목 역이 된다.

**무대 글자의 문자열은 `meme-lines.ts`에 둔다.** 픽셀 폰트 서브셋은 `speech-lines.ts` ·
`meme-lines.ts` · `share-copy.ts` **셋에서만** 글자를 긁어가므로(`scripts/build-font.mjs`),
`meme-scenes.ts`에 박은 글자는 **폰트를 다시 구워도 시스템 폰트로 떨어진다.** 실제로
"일희일비 금지"의 `희`가 다른 어디에도 안 쓰이는 글자라 여기 걸렸다.

**글자 폭 합이 정수가 아니면 픽셀 폰트가 흐려진다.** `drawLabels`는 가운데 정렬로 한 번에
찍으므로, 폭이 소수면 모든 글자가 소수 좌표에 앉는다. Galmuri11 기준 한 자 폭은
한글 `11 × unit`px · 숫자 `7.33 × unit` · 공백 `4.58 × unit`이라 — **공백과 어중간한
unit이 범인이다.** "일희일비 금지"를 라벨 둘("일희일비" · "금지")로 나누고 "장 시작!"의
공백을 뺀 게 그래서고, 시계가 `unit: 12`인 것도 그렇다 (unit 8은 숫자 폭이 58.67px다).
**새 크기를 쓰면 `LABEL_UNITS`에 더할 것** — 안 더하면 첫 프레임에서 그 글자만 시스템
폰트로 떴다가 뒤늦게 바뀐다. `drawLabels`는 `\n`을 모르니 여러 줄은 라벨을 여러 개 넘긴다.

#### 멀어져도 같은 물건이어야 한다

열차 한 칸은 18유닛으로 잡아 **역(3배)과 주행(1배)이 같은 그림을 쓴다.** 가까울 때와
멀 때 다른 열차를 그리면 줌아웃하는 동안 열차가 다른 물건으로 바뀐다.

**칸은 곧게 그린다 — 기울이지 말 것.** 도트 그림은 회전을 못 해서 세로로 밀어 기울여
봤는데, 밀기는 길이를 늘여서 칸이 사선 얼룩이 됐다 (열차가 열차로 안 보이면 기울기를
맞춘 보람이 없다). 대신 **선로 쪽을 손본다.**

- **오르막은 눕힌다** (가로 95 · 낙차 0.32). 기울기가 세면 곧은 칸을 레일이 관통한다.
- **내리막은 거의 절벽으로 세운다** (가로 10 · 낙차 0.44). 그러면 칸이 곧은 채로 뚝
  떨어져 기울일 일이 아예 없어지고, 낙차도 제일 크게 보인다 — 앞칸이 절벽 아래에
  닿았는데 뒷칸은 아직 위에 있는 그림이 이 판의 한 장면이다.
- 레일은 칸보다 **먼저** 그린다. 칸 밑에서 가려져야 남은 어긋남이 눈에 안 걸린다.
- 칸은 **둘만** 붙인다. 길수록 곡선에서 어긋난 게 눈에 띈다.

**한 화면에 꼭지는 둘까지만.** 오르막+내리막 한 쌍을 화면 폭(108칸) 언저리로 잡는다 —
좁게 두면 오르내림이 서너 개씩 겹쳐 무슨 움직임인지 안 읽힌다.

#### 영상은 화면의 그 캔버스를 녹화한다

`canvas.captureStream(30)` + MediaRecorder ([video-export.ts](apps/web/src/lib/video-export.ts)).
**미리보기와 결과물이 같은 캔버스다** — 내보내기용으로 장면을 다시 그리면 눌러 받아보기
전에는 뭐가 나올지 알 수 없게 된다.

- **딱 한 바퀴만 굽는다.** 장면은 루프라 시작과 끝이 같은 그림이고, 한 바퀴면 SNS에서
  반복 재생될 때 이음매가 안 보인다. 말풍선도 루프 끝에서 투명도가 0이 되게 맞춰뒀다.
- **그릇은 mp4를 먼저 고른다.** webm은 인스타·카톡이 못 받아서, 열리지 않으면 만든 의미가
  없다. 사파리는 mp4로, 크롬은 상황에 따라 webm으로 떨어진다.
- **누르면 바로 공유하지 않는다** — 굽는 시간이 iOS의 사용자 제스처를 이미 삼켰다.
  다 구운 뒤 시트를 띄우고 거기서 다시 누르게 한다 (인스타 내보내기와 같은 이유).
- 영상을 못 굽는 브라우저에서는 버튼이 **한 컷 PNG 저장**으로 바뀐다. 여기서도
  브라우저 사정은 화면에 안 적는다 — 되는 일을 할 뿐이다.

목업에는 들어 있지 않다 (`entry.tsx`가 `App`만 번들한다). 서버가 필요 없는 화면이라
넣을 수는 있지만, 목업은 "계산기 한 판"을 보여주는 물건이라 짤은 뺀다.

### 하단 한마디

화면 맨 아래에 시간을 보고 등을 떠미는 한 줄이 붙는다
([apps/web/src/components/nudge-lines.ts](apps/web/src/components/nudge-lines.ts)).
상단 카운터·개미 말풍선과 **같은 시간 축**을 쓴다 — 구간 경계(`nudgeTier`)를 여기 한 곳에
두고 셋이 함께 가져다 쓴다. 같은 −5%라도 100만원어치면 20분이고 1억어치면 사흘이라
들어야 할 말이 다르다.

**구간은 노동의 단위로 일곱으로 끊는다** — 10분 / 1시간 / 1근무일(8h) / 1근무주 /
1근무월 / 반년. 세 번째 경계가 표기가 "출근 N번"으로 바뀌는 지점(`WORK_UNIT_TIER`)과
같은 값이라 상단 문구의 은유 규칙이 여기에 얹힌다. 예전엔 넷(1시간/8시간/24시간/닷새)
이었는데 **맨 위 구간에 상한이 없어** 닷새치와 반년치가 같은 풀을 썼다 — 3주치 손익에도
"평생 안 나가도 돼"가 떴다. 위아래로 하나씩 늘려 폈다.

셋이 같은 축이어도 겹치지 않는 건 **역할과 소재를 함께 갈라놨기 때문**이다.
역할만 나누면 소재가 겹쳐 세 줄이 한 얘기를 세 번 한다.

| | 역할 | 소재 |
| --- | --- | --- |
| 상단 | "{시간} 만큼 __"을 끝맺는 서술어 | 노동 동사구만 |
| 말풍선 | 개미 1인칭 혼잣말 | 개미의 몸과 살림 — 다리·더듬이·등껍질, 굴·먹이·여왕개미 |
| 하단 | 제3자 시선의 배경 문장 | 생활 — 커피·점심·휴가·월급·사표, 그리고 훈수 |

뽑는 시점은 **구간이나 손익 방향이 바뀔 때뿐이다** (셋 다 같은 이유).
이 문구들은 본문 폰트로 그리므로 픽셀 폰트 서브셋 대상이 아니다 — 고쳐도
`pnpm --filter @yca/web font`를 다시 돌릴 필요가 없다. 그래서 `speech-lines.ts`와
한 파일에 두지 않았다.

무일푼일 때의 "최저시급 기준" 캡션은 **이 한마디 아래, 화면 제일 밑**에 둔다.
카운터 위에 붙이면 숫자보다 먼저 읽힌다.

### 문구 검사

문구는 코드가 아니라 글이라 타입 검사가 아무것도 안 잡아준다. 그런데 깨지는 건 늘 같은
몇 가지라 규칙으로 적을 수 있고, 실제로 한 번씩 다 터졌다 —
[scripts/check-lines.mjs](apps/web/scripts/check-lines.mjs)가 그걸 검사한다.

- 상단 풀에 **기간명사**가 있는가 ("{43일 22시간} 만큼 **평생** 안 나가도 돼")
- 상단 풀이 **3구간 아래에서 출근 은유**를 쓰는가 ("{23분} 만큼 안 **나가도** 돼")
- 세 surface가 **같은 구간에서 같은 문장**을 내는가
- 풀 길이가 `TIER_COUNT`와 맞는가, 구간마다 5줄 이상인가, 대문/대기/본전 풀이 겹치는가
- 짤 일곱 판이 **서로 같은 말**을 하는가, **앱 말풍선과 겹치는가** (같은 개미의 같은 자리다)
- 짤 문구의 **한 줄이 14자를 넘는가** — 9:16에서 두 줄을 넘기면 말풍선이 개미보다 커진다

`pnpm --filter @yca/web lines`로 돌리고 **`build`·`vercel-build`가 먼저 부른다** —
문구는 배포돼야 보이는 물건이라 사람이 기억해서 돌리는 검사로는 안 걸린다.
새 규칙이 생기면 주석이 아니라 여기에 적을 것.

### 물타기 / 불타기

개미를 탭하면 추가 매수 시트가 열린다. 먼저 답하는 질문은 **"몇 주를 더 사야 평단이
넘어오나"**다 (`halfwayAveragingPlan()`).

평단을 현재가까지 끌어내리는 건 **불가능하다** — 아무리 사도 수렴만 할 뿐 절대 닿지
않는다. 그래서 목표를 평단과 현재가의 **딱 중간**으로 잡는다. 필요한 수량은 (수학적으로)
지금 들고 있는 수량과 같다. 임의의 목표 평단이 필요하면 `sharesToTargetAverage()`가
Q를 풀어주고, 닿을 수 없는 목표에는 null을 준다.

물타기/불타기는 **입력한 단가**가 정한다 (현재가가 아니다). 사용자가 다른 값을 넣으면
그쪽을 따라가야 확인 버튼 문구("설마 물탔어? / 우와 불탔어? 평단 바꾸기")가
거짓말을 안 한다. 시트 안의 `--mood-color`도 같은 이유로 **추가 매수 이후**의 손익을
따른다 — 바깥 화면의 지금 손익을 물려받으면 뒤집히는 순간 색이 어긋난다.

확정 계산은 여전히 `averageIn()` 하나다.

**추가 매수는 이미 난 손익을 안 바꾼다.** 손익 변화량은 정확히
`(현재가 − 매수 단가) × 추가 수량`이라, 시트가 기본값으로 넣어두는 **현재가**로 사면
0이다 — 평단과 수량만 움직이고 카운터·장대봉·하단 한마디는 제자리다. 이게 이 앱에서
제일 오해받는 지점이다: 시트를 닫은 사용자가 "평단 바꾸기가 안 먹었다"고 읽는다.
그래서 시트의 시간 행은 **적힌 글자가 같으면 취소선으로 두 번 적지 않고** "그대로"로
한 번만 적고, 왜 안 줄어드는지(현재가보다 싸게 사야 줄어든다) 한 줄로 알린다.
비교는 초가 아니라 `formatWorkSpanWords()`의 **결과 문자열**로 한다 — 초가 미세하게
달라도 "출근 131번"이 그대로면 사용자에게는 안 바뀐 것이다.

같은 이유로 **확인 직후에는 종목 카드로 데려간다** (`changeNote` → `scrollIntoView`
+ `[data-changed]` 테두리 깜빡임 + 카드 안 안내 줄이 "평단 A → B · N주로 바꿨어"로
6초간 바뀐다). 바뀌는 값 셋(평단·수량·수익률)이 전부 화면 맨 아래 카드 안에 있는데
개미는 맨 위에 있어서, 시트를 닫으면 **안 바뀌는 것들(카운터·장대봉)만 보인다.**
이 안내가 없을 때 실제로 "평단 바꾸기가 안 먹는다"는 신고가 나왔다.

**시트가 떠 있는 동안 뒤 화면은 스크롤을 잠근다** (`useLockedBodyScroll()`). 딤은
시각적으로만 덮을 뿐이라 그 위를 쓸면 대시보드가 따라 움직인다. iOS Safari는 body에
`overflow: hidden`이 안 먹으므로 `position: fixed`로 못 박고, 그러면 스크롤 위치가
0으로 튀니 열 때 위치를 기억했다 닫을 때 되돌린다 — **되돌리는 쪽을 지우지 말 것**,
시트를 닫는 순간 화면이 맨 위로 올라간다. 시트 안쪽 스크롤이 끝에 닿아 뒤로 넘어가는
건 `overscroll-contain`이 막는다.

### 측정 (GA4)

**화면 이동이 거의 없는 앱이라 페이지뷰만으로는 `/` 하나밖에 안 남는다.** 온보딩
세 단계가 전부 클라이언트 상태라 라우트가 안 바뀌기 때문이다. 그래서 온보딩이 어디서
끊기는지는 이벤트로만 보인다 ([apps/web/src/lib/analytics.ts](apps/web/src/lib/analytics.ts)).

**연봉·시급·평단가·수량·손익 금액은 절대 파라미터에 넣지 않는다.** 재무 정보가
기기 밖으로 안 나가는 건 이 앱의 규칙이고(공유 링크조차 종목명·시간·단계·손익여부만
싣는다) GA도 예외가 아니다. `EventParams`가 이벤트마다 보낼 모양을 못으로 박아뒀으니
새 이벤트는 거기 먼저 적고 쓴다 — `track()`은 임의의 객체를 안 받는다. 그래서 금액
대신 성질만 보낸다: 연봉은 `"entered" | "broke"`, 물타기는 `"down" | "up"`.

| 이벤트 | 언제 |
| --- | --- |
| `salary_submit` | 연봉 단계 "다음" |
| `ticker_search_miss` | 검색 결과 0건 (오타 + 미지원 종목) |
| `ticker_select` | 종목 고름 |
| `onboarding_complete` | 평단·수량까지 넣고 결과 화면 도달 |
| `share_click` | 공유하기 (`web_share` / `clipboard`) |
| `story_open` / `story_export` | 인스타 시트 엶 / 실제 내보냄 (`share` / `download`) |
| `share_cta_click` | 공유 링크에서 "다시 계산해보기" |
| `averaging_open` / `averaging_confirm` | 개미 탭 / 평단 바꾸기 |
| `profile_reset` | 다시 입력 |
| `meme_open` | 짤 공장 들어옴 (손익 화면을 안 거쳐도 열려서 따로 센다) |
| `meme_record` / `meme_export` | 영상 굽기 시작 / 실제 내보냄 (`share`·`download` × `video`·`image`) — 어느 탭에서 나갔는지는 `meme_format`이 말해준다 |

`ticker_search_miss`는 결과가 멎고 **1.2초를 더 기다린 뒤** 보낸다 — 안 그러면
"삼성전자"를 치는 동안 "삼"·"삼성"·"삼성전"이 전부 0건 구간을 지나가며 찍힌다.

**gtag를 직접 깐다** (`@next/third-parties`의 `GoogleAnalytics`를 안 쓴다). GA4는
페이지뷰에 현재 주소를 자동으로 싣는데 그게 `/s/eyJuIjoi...`라 **공유 토큰이 통째로
GA에 쌓인다** — base64일 뿐이라 풀면 종목명·시간·레벨이 나온다. 파라미터에서 금액을
막아놓고 주소로 흘리는 셈이라, `gtag('config', …)`에 **토큰을 뗀 주소**(`/s`)를 직접
넣어 보낸다. 링크에서 대문으로 넘어올 때의 referrer도 같이 뗀다. 공유 링크로 들어온
사람 수는 그대로 잡히고 **어느 링크였는지만** 안 남는다.

**측정 ID(`G-RXGLBBZ0TY`)는 [layout.tsx](apps/web/src/app/layout.tsx)에 박아뒀다.**
gtag 스크립트 태그로 페이지 소스에 그대로 드러나는 값이라 환경변수로 숨겨봐야 의미가
없고, 대신 Vercel 환경변수를 안 건드려도 배포하면 바로 켜진다. 바꾸려면 여기만 고친다.
`NEXT_PUBLIC_GA_ID`를 넣으면 그쪽이 이긴다 (다른 속성으로 시험할 때).

**개발 모드(`NODE_ENV !== "production"`)에서는 스크립트를 안 싣는다** — `pnpm dev`로
화면 만지며 누른 게 섞이면 온보딩 전환율이 통째로 거짓말이 된다. 그때 `track()`도
`window.gtag`가 없어 조용히 지나간다. **단일 HTML 목업**도 layout.tsx를 안 태우므로
같은 길이고(esbuild가 `entry.tsx`부터 번들한다), 광고 차단기도 마찬가지다.

GA4는 커스텀 파라미터(`ticker_name`, `search_term`, `salary_kind` …)를 **관리 →
맞춤 정의에 등록해야** 보고서에 뜬다. 등록 전에는 실시간 보고서에서만 보인다.

공유 화면의 CTA만 클라이언트 컴포넌트로 떼어낸 것도 이 때문이다
([share-cta.tsx](apps/web/src/app/s/[snapshot]/share-cta.tsx)) — 서버 컴포넌트는
`onClick`을 못 넘긴다.

## 구조

```
apps/web        Next.js 15 (App Router, React 19, Tailwind v4) — 포트 3000
apps/api        Fastify 5 (TypeScript, ESM) — 포트 4300
packages/shared @yca/shared — web/api가 공유하는 zod 스키마와 계산 로직
```

패키지 이름은 `@yca/*` 스코프를 쓴다 (`@yca/web`, `@yca/api`, `@yca/shared`).

## 명령어

루트에서 실행한다.

```bash
pnpm install       # 의존성 설치
pnpm dev           # shared 빌드 후 web+api+shared watch 동시 실행
pnpm dev:web       # 웹만
pnpm dev:api       # API만
pnpm typecheck     # 전 패키지 tsc --noEmit
pnpm build         # shared → apps 순서로 프로덕션 빌드

pnpm --filter @yca/web lines   # 랜덤 문구 풀 검사 (build가 먼저 부른다)
pnpm --filter @yca/web font    # 픽셀 폰트 서브셋 재빌드 (speech-lines.ts 고치면)
pnpm --filter @yca/web mockup  # 단일 HTML 목업 (개발 서버 켜둔 채로 돌려도 된다)
```

`pnpm dev*` / `pnpm typecheck` / `pnpm build`는 모두 **먼저 `@yca/shared`를 빌드**한다.
shared는 `dist/`를 배포하므로 빌드 전에는 web/api가 임포트를 해소하지 못한다.

## 규칙

**종목과 전일 종가는 실제, 장중 현재가는 아직 없다.** KRX Data Marketplace
OPEN API([apps/api/src/krx.ts](apps/api/src/krx.ts))가 주는 건 **일별 확정
데이터뿐이라 장중 현재가가 없다.** 예전엔 그 종가 위를 1초 틱 랜덤워크가 걷게 해
장중 시세를 흉내 냈지만, **가짜로 움직이는 시세가 실제로 오해돼 걷어냈다.** 지금은
`MockMarket`이 **현재가 = 전일 종가**로 고정하고, 화면이 현재가 옆에 "실시간 적용
준비중 · 전일 종가" 라벨을 붙인다([dashboard.tsx](apps/web/src/components/dashboard.tsx)).
카운터·장대봉·개미는 전부 이 현재가로 계산되므로, 종가 기준 실제값으로 서 있되
1초마다 흔들리지 않는다.

실시간(장중) 시세를 붙일 때 갈아끼울 곳은 `MockMarket`이지 `krx.ts`가 아니다 —
종목 마스터는 그대로 쓴다. 그때 `subscribe`가 틱마다 listener를 부르면 SSE가 다시
살아난다 (라우트·`useQuote`·목업은 이미 그 인터페이스에 붙어 있다).

목 엔진이 `packages/shared`에 있는 건 API(Node)와 단일 HTML 목업(브라우저)이
**같은 엔진**을 돌리기 위해서다. 반대로 `krx.ts`는 인증키를 쥐므로 **api에만 둔다** —
shared로 옮기면 브라우저 번들에 키가 딸려 들어간다. 실시간 시세를 붙일 때는
`MockMarket`과 같은 모양(`search` / `getQuotes` / `subscribe`)의 구현체로 갈아끼우면
라우트와 웹은 그대로 둔다.

`KRX_AUTH_KEY`가 없거나 호출이 실패하면 `MOCK_TICKERS` 20종목으로 조용히 떨어진다
(로그 한 줄은 남긴다) — 키 없이도 개발이 굴러가야 하기 때문이다. 인증키는 마이페이지
발급과 **별개로 "유가증권/코스닥 일별매매정보" 서비스를 각각 이용 신청**해야 하고
1년마다 갱신한다. 하루 10,000회 제한이지만 날짜 하나로 전 종목이 통째로 오므로
기동 때 시장당 1회씩만 쓴다. **종가는 기동 때 한 번만 받는다** — 갱신하려면 서버를
다시 띄운다.

KRX 응답은 값이 전부 **문자열이고 숫자에 천 단위 콤마가 들어있다.** 스펙 문서가
로그인을 요구해 필드명(`ISU_CD`/`ISU_NM`/`TDD_CLSPRC`/`MKTCAP`)은 공개 예제로 확인한
것이라, 어긋나면 `loadKrxTickers()`가 첫 행의 키를 로그로 뱉고 목으로 떨어진다.

**단일 HTML 목업은 `pnpm --filter @yca/web mockup`으로 굽는다.** 실제 컴포넌트와
계산 로직을 그대로 번들하고, 서버가 필요한 네 지점만 alias로 바꿔 끼운다
(`@/lib/use-quote`, `@/lib/tickers`, `@/lib/share-url`, `@/lib/story-image`
→ `apps/web/mockup/`).
**목업용으로 화면을 복제하지 말 것** — 두 벌이 되는 순간 어긋난다.
새 컴포넌트가 서버를 필요로 하면 그 접점을 `src/lib/`의 모듈 하나로 뽑고 alias에 추가한다.

목업 빌드는 Tailwind CSS를 뽑으려고 `next build`를 돌린다. 예전엔 이게 `pnpm dev`가
쓰는 `.next`를 덮어서 **개발 서버를 먼저 꺼야 했다** — 켜둔 채로 돌리면 빌드가 실패하거나,
통과하고도 개발 서버가 죽거나, **화면이 통째로 빈 채 뜬다**(청크가 404). 지금은
`NEXT_DIST_DIR=.next-mockup`으로 딴 폴더에 뽑으므로 **켜둔 채로 구워도 된다.**

`next.config.ts`의 `distDir`가 그 문을 열고, 딴 폴더로 뽑을 때의 뒤처리는
[scripts/next-build.mjs](apps/web/scripts/next-build.mjs)가 맡는다 — `next build`가
**`next-env.d.ts`와 `tsconfig.json`을 dist 폴더에 맞춰 고쳐 쓰므로** 구운 뒤 되돌려
놓는다 (안 되돌리면 추적 중인 파일 둘이 없는 폴더를 가리킨 채 커밋에 딸려 간다).

**확인용 프로덕션 빌드는 `pnpm --filter @yca/web build:check`로 돌린다.** 그냥
`pnpm build`를 돌리면 개발 서버의 `.next`를 덮어, 다음에 화면을 열었을 때 아무것도
안 뜬다. 배포(Vercel)는 `NEXT_DIST_DIR`을 안 넣으니 그대로 `.next`에 쌓인다.

**공유 계약은 `packages/shared`에 둔다.** API 응답 모양은 zod 스키마로 정의하고,
API는 그 타입을 반환 타입으로, 웹은 `schema.parse()`로 응답을 검증한다.
타입을 양쪽에 따로 적어두지 않는다 — 한쪽만 고치면 조용히 어긋난다.

**shared는 ESM + NodeNext로 빌드된다.** `packages/shared` 안의 상대 임포트는
반드시 `.js` 확장자를 붙인다 (`./status.js`). 확장자를 빼면 빌드는 통과하지만
런타임/번들러에서 모듈을 못 찾는다. api 소스도 동일하다.

**SVG 색은 `hsl()`에 정수만 넣는다.** OG 이미지를 굽는 resvg는 소수점 hue를
거부하고 fill을 **검정으로 떨군다** (브라우저는 멀쩡히 그려서 눈치채기 어렵다).
`antPalette()`의 `hsl()` 헬퍼가 반올림을 담당하니 우회하지 말 것.

**SSE 라우트는 CORS 헤더를 직접 실어야 한다.** `reply.raw.writeHead()`가
`@fastify/cors`가 붙인 헤더를 통째로 덮어쓴다. `/quotes/stream`의
`allowedOrigin()`이 그래서 존재한다 — 지우면 브라우저에서 시세가 안 들어온다.

**개발 모드에서는 CORS를 열어둔다.** 실기기로 LAN IP 접속해 테스트해야 하기 때문이다.
배포 시에는 `env.ts`의 `ALWAYS_ALLOWED_ORIGINS`(서비스 중인 웹 주소)와
`WEB_ORIGIN`(쉼표 구분)을 **합친 목록**만 허용된다.

**서비스 주소를 코드에 박아둔 건 의도다.** 공개된 주소라 숨길 게 없고, CORS가
배포 대시보드의 손입력 하나에 매달리면 안 되기 때문이다 — 실제로 Render의
`WEB_ORIGIN`이 `http://localhost:3000`으로 남아 배포된 사이트가 제 API를 통째로
못 부른 적이 있다. 대시보드는 값을 눈으로 확인하기 어렵고(같은 키가 두 줄이어도
막지 않는다) 고치면 재시작을 기다려야 해서 원인을 늦게 찾는다. `WEB_ORIGIN`은
**덮어쓰기가 아니라 추가**이므로 커스텀 도메인·프리뷰 배포를 더할 때 쓴다.

**개발에서 API 주소는 `localhost`로 못박지 않는다** ([api.ts](apps/web/src/lib/api.ts)).
`NEXT_PUBLIC_API_URL`이 없으면 **지금 보고 있는 호스트**의 4300으로 부른다 — 폰에서
`192.168.x.x:3000`으로 들어오면 localhost는 맥이 아니라 **그 폰 자신**이라 아무 데도
안 닿는다. 실기기 테스트를 위해 CORS를 열어둔 것과 짝을 이루는 규칙이다.

**서버가 안 잡히면 검색 화면이 "다시 시도" 버튼을 띄운다** (`SearchStep`). Render 무료
API는 15분마다 잠들어 첫 요청이 그냥 실패하는 일이 잦은데, 그때 화면에 남는 게 안내
문구뿐이면 새로고침 말고는 할 게 없다. 검색어가 그대로여도 다시 부르도록 `retry`
카운터를 효과의 의존성에 넣어뒀다.

**기다리는 중을 "없음"으로 보이게 두지 말 것** (`pending` → `WaitingNote`). 잠든 API가
깨는 데 1분쯤 걸리는데, 그 사이 화면이 결론을 내버렸다. 인기 종목 자리는 통째로 비어
**목록이 없는 앱처럼 보였고**("주식 목록이 안 보인다"), 검색어를 친 사람에게는 **"찾는
종목이 없어 · 국내 주식만 지원해"가 떴다** — 삼성전자를 못 찾는다고 거짓말을 하는 셈이라
기다리는 대신 앱을 닫게 된다. 실패도 0건도 아닌 **세 번째 상태**라 플래그를 따로 둔다.
3.5초를 넘기면 왜 오래 걸리는지("서버가 자고 있었나 봐")까지 적는다 — 이유를 모르면
기다릴 마음이 안 생긴다.

**단, 이미 목록이 있으면 기다리는 티를 내지 않는다.** 한 글자 칠 때마다 "불러오는 중"이
끼어들면 결과를 눈으로 좇을 수가 없다 (`pending`이 `tickers.length === 0`을 함께 보는
이유). 옛 결과를 두고 조용히 갈아끼운다.

**포트는 3000(web) / 4300(api).** 이 머신에는 다른 개발 서버가 3100·3300·4000~4102·4202를
이미 점유하고 있어서 4300을 골랐다. 바꿀 때는 `apps/api/src/env.ts`,
`apps/web/src/lib/api.ts`, 두 `.env.example`을 함께 고친다.

**종목 로고는 네이버 금융에서 종목코드로 끌어온다** — 실패는 정상 경로다
([apps/web/src/components/ticker-logo.tsx](apps/web/src/components/ticker-logo.tsx)).
`https://ssl.pstatic.net/imgstock/fn/real/logo/stock/Stock{종목코드}.svg`는 **비공식**이라
언제든 막힐 수 있고, 신규 상장·스팩·리츠는 404가 온다. 그래서 로고는 색 타일 위에
얹고, `onError`가 뜨면 조용히 타일이 드러난다. `<img>`를 화면 곳곳에 흩뿌리지 말고
`TickerLogo` 하나를 거칠 것 — 소스를 바꾸거나 로고를 끌 때 여기만 고치면 된다.
단일 HTML 목업은 오프라인일 수 있어 로고가 안 떠도 같은 타일로 떨어진다.

**수량은 드롭다운이 아니라 바텀시트 휠로 고른다**
([quantity-picker.tsx](apps/web/src/components/quantity-picker.tsx)). `<select>`는
iOS에서만 바닥 휠로 열리고 데스크톱·안드로이드에서는 드롭다운으로 떨어져 화면마다 다른
물건이 된다. 고른 값은 **가운데 칸에 놓인 줄**이라 스크롤 위치를 줄 높이로 나눠 되짚는다 —
`ROW_PX`와 CSS 줄 높이가 어긋나면 엉뚱한 값이 잡힌다. 시트는 **body에 포털로 건다**:
피커가 놓이는 자리가 `<label>` 안이라 거기서 그리면 시트 속 버튼을 누른 것도 라벨을
누른 것으로 읽힌다. 목록에 없는 수량을 위해 맨 아래 "직접 적기"를 남겨둔다.

**여닫는 상태는 피커가 아니라 부르는 쪽이 쥔다.** 이 시트는 제 버튼으로만 열리지
않는다 — 평단가를 다 친 순간 이어서 뜨는 게 온보딩의 흐름이라, "언제 열리나"는 흐름을
아는 쪽(`PositionStep`)이 정해야 한다.

### 평단가 → 수량, 한 줄기로

평단가 화면은 **들어오자마자 평단가 칸에 포커스가 가고**(`autoFocus`) 키패드가 뜬다.
이 화면에서 제일 먼저 할 일이 그거라서다. 그리고 평단가를 다 치면 **수량 시트가 이어서
열린다** — 두 입력이 한 줄기로 이어져야 "평단이 어떻게 돼? 몇 주나 산 거지?"라는 물음이
한 번에 끝난다.

이어주는 신호는 **키패드의 확인 키**다 (아래 "커스텀 숫자 키패드" 참고). 같은 자리가
수량을 고르기 전에는 **"다음"**(수량 시트를 연다), 고른 뒤에는 **"입력"**이다.

- **수량을 한 번은 반드시 거치게 한다** (`quantityPicked`). 수량 칸에는 처음부터 "10"이
  적혀 있어서, 안 물으면 **아무도 안 고른 10주가 조용히 확정된다.** 반대로 한 번 고른
  뒤 평단가를 고치러 돌아왔을 땐 되묻지 않고 바로 "입력"으로 간다.
- **아래 버튼줄도 같은 말을 한다.** 자리가 둘이어도 지금 할 일은 하나다.
- **세 갈래가 한 함수(`advance()`)로 모인다** — 키패드 확인 키, 아래 버튼, 엔터
  (하드웨어 키보드). 갈라놓으면 어느 길로 왔느냐에 따라 다른 일이 난다.
- **곁줄(전일 종가·매수 원금)은 두 줄 자리를 처음부터 잡아둔다**(`min-h-10`). 굴려서
  보여주는 건 키패드가 뜨는 그 한 번뿐인데 그때 곁줄은 아직 한 줄이라, 평단가를 다 쳐서
  둘째 줄이 생기면 그만큼 밀려 키패드 뒤로 들어간다 — 방금 친 값의 결과가 안 보인다.

**미지원 종목은 검색에서 조용히 빠진다.** KRX 국내 마스터에 없는 해외 주식·ETF는
`search`에 아예 안 잡히므로, 검색 결과가 비면 "국내 주식만 지원해" 안내로 대신한다
([onboarding.tsx](apps/web/src/components/onboarding.tsx) `SearchStep`). 오타와 진짜
미지원을 구분할 방법이 없어 한 문구로 합쳤다.

**인기 종목 롤링은 transform이 아니라 `scrollTop`으로 굴린다** (`PopularRoller`).
저 혼자 흘러가되 **손으로도 밀 수 있어야** 하는데, transform으로 올리면 스크롤할 게
없어서 손가락이 목록을 통과해 페이지를 민다. 스크롤로 굴리면 관성도 튕김도 브라우저가
공짜로 해주고, 손이 닿으면(`pointerdown`·`wheel`·직접 스크롤) 멈췄다가 2.5초 조용하면
다시 굴러간다. **굴러간 거리는 JS 변수에 소수점째로 쌓는다** — `scrollTop`을 읽어 더하면
브라우저가 정수로 반올림해 돌려주므로 한 프레임치(0.3~0.6px)가 매번 0으로 깎여 목록이
영영 안 움직인다.

**숫자 입력칸 옆에는 한글 읽기를 붙인다** (`koreanWon()`). 연봉은 만원 단위라 한 자리만
밀려도 열 배가 어긋나고, 평단가는 0이 여섯 개씩 붙어 24000과 240000이 한눈에 안 갈린다.
연봉칸은 "만원" 자리를 읽기가 대신하고(적고 나면), 평단가·시트 단가는 라벨 오른쪽에 붙는다.

**금액 입력 상한은 100억원** (`MAX_INPUT_WON`). 계산이 틀려서가 아니라 **입력이 말이
안 되기 때문**에 둔다 — 연봉칸은 만원 단위라 0 하나만 더 붙어도 자릿수가 통째로 밀리는데,
그 값도 화면은 멀쩡한 얼굴로 받아 시급 몇백만원짜리 결과를 내놓는다. 넘으면 **되돌리지 않고
상한값으로 바꿔 넣는다** (`capNumericInput`) — 글자가 안 들어가면 고장으로 읽히지만
상한값이 들어차면 "여기까지"가 그대로 읽힌다. 연봉칸은 만원 단위라 상한도 환산해 쓴다.

**자릿수가 아니라 금액을 읽는다.** 연봉칸의 "12,000"을 자릿수대로 읽어 "만 이천"이라
적고 뒤에 "만원"을 붙이면 "만 이천 만원"이 되는데 실제로는 1억 2천만원이다. 그래서
만원 단위는 원으로 곱해 넘기고 `koreanWon()`이 억·만으로 끊어 읽는다. 띄어쓰기는
맞춤법대로 **만·억 묶음 사이만** 띄우고("일억 이천만원"), 단위 '원'은 좁은 곁줄이라 붙인다.

### 커스텀 숫자 키패드

**숫자 입력은 네이티브 키보드를 부르지 않고 화면에 직접 그린 키패드로 받는다**
(`inputMode="none"` +
[numeric-keypad.tsx](apps/web/src/components/numeric-keypad.tsx)). 연봉·평단가·수량
직접 입력·추가 매수 시트 네 곳이 전부 이걸 쓴다. 네이티브를 버린 이유는 둘이다.

1. **iOS 숫자 키패드에는 엔터 키가 아예 없다.** 버튼이 키보드에 가리면 진행이 통째로
   막힌다 (키보드를 내려야 보이는 걸 알아채는 사람은 드물다). **웹은 네이티브 키보드에
   키를 못 붙인다** — `enterkeyhint`는 있는 리턴 키의 이름표만 바꾼다. 예전엔 키보드가
   올라올 때 버튼을 굴려 보여주고(`useRevealOnKeyboard`) 평단가 화면엔 키보드 위에
   바(`.kb-bar`)를 띄우는 우회로가 화면마다 필요했다.
2. **iOS가 키보드 옆에 띄우는 자동완성 알약(비밀번호·카드·주소)을 웹이 못 끈다** —
   연봉을 적는 칸에 카드 자동입력이 떴다. 키보드를 안 부르면 같이 사라진다.

키패드 맨 아랫줄은 **[지우기][0][확인]**이라 입력과 확정이 키패드 안에서 끝난다.
확인 키는 화면 아래 실제 버튼과 **같은 말을 해야 한다** ("다음"/"입력"/"바꾸기").
값 편집은 끝자리에서만 하고, 상한·쉼표는 입력칸 onChange와 같은 `capNumericInput`을
부르는 쪽이 씌운다 — 키패드로 치든 하드웨어 키보드로 치든 같은 규칙을 타야 한다.

**여닫는 상태는 부르는 쪽이 쥔다** (수량 피커와 같은 이유). 칸의 focus가 열고,
▾·흐름(수량 시트 열기·제출)이 닫는다 — **블러에 닫기를 걸지 말 것.** VoiceOver처럼
pointerdown 없이 클릭만 합성하는 경로에서 키가 눌리기 전에 키패드가 사라진다. 접을 때는
포커스도 같이 거둬야 다음 탭에서 focus 이벤트가 다시 온다 (칸의 `onClick`이 이중 안전망).

키패드가 화면 요소라 시각 뷰포트는 안 줄어든다 — 가려지면 안 되는 묶음을 `revealRef`로
넘기면 뜰 때 한 번 키패드 위로 굴려주고, 화면 끝에는 `.keypad-space`로 키패드 높이
(`--keypad-h`, 키 치수를 바꾸면 같이 고칠 것)만큼 자리를 비워 굴릴 공간을 만든다.
**시트는 페이지가 아니라 시트 안쪽이 굴러야 한다** — 시트가 떠 있는 동안 뒤 화면은
잠겨 있어서 `window.scrollBy`가 아무 일도 안 한다 (`scrollableAncestor`가 찾는다).

**네이티브 키보드가 뜨는 곳은 글자 키보드를 쓰는 종목 검색뿐이다.** 그 화면만
`useKeyboardInset`(→ `--keyboard-inset` → `.kb-pad`)이 가려진 높이만큼 아래를 늘린다.

엔터는 form으로 받는다 — 하드웨어 키보드(숫자 칸도 `inputMode="none"`일 뿐 타이핑은
받는다), 그리고 글자 키보드를 쓰는 종목 검색(맨 위 결과를 고른다)에서 동작한다.

**form 안의 버튼에는 `type`을 반드시 적는다.** 안 적으면 `submit`이 기본값이라,
엔터가 확인 버튼이 아니라 **form의 첫 submit 버튼**으로 간다. 실제로 추가 매수 시트에서
엔터를 치면 "10주 넣어보기"가 눌려, 화면에는 15주라고 적혀 있는데 20주가 저장됐다.

**연봉·보유종목은 localStorage에만 둔다.** 서버로 보내지 않는다
([apps/web/src/lib/storage.ts](apps/web/src/lib/storage.ts)).

**tsconfig는 루트 `tsconfig.base.json`을 상속한다.** `strict`에 더해
`noUncheckedIndexedAccess`, `noUnusedLocals`가 켜져 있다. 개별 패키지에서 끄지 않는다.

## 배포

**웹은 Vercel, API는 Render.** 저장소 하나(`parmirang/Yun-cha-Ants`)를 두 서비스에 각각
연결한다. 둘 다 무료 등급으로 굴러간다.

`packages/shared/dist`는 **gitignore라 새 클론에 없고**, `@yca/shared`에는 `prepare`
스크립트가 없다. 그래서 **어느 쪽이든 빌드 첫 줄이 shared 빌드여야 한다** — 안 그러면
임포트가 안 풀린다. 웹은 `vercel-build` 스크립트가 그 일을 한다 (Vercel이 기본
`next build`보다 이걸 먼저 집는다). 로컬 `build`는 루트 스크립트가 순서를 잡으므로
그대로 둔다.

**Render의 Root Directory를 `apps/api`로 잡지 말 것.** Render는 rootDir 바깥 파일을
빌드에서 못 보는데 `@yca/api`는 `workspace:*`로 `packages/shared`에 의존한다.
저장소 루트에서 빌드하고 `--filter`로 대상만 고른다 ([render.yaml](render.yaml)).

`env.ts`는 이미 `process.env.PORT`를 읽고 host를 `0.0.0.0`으로 둔다 — 로컬 4300은
개발 포트일 뿐이라 배포용으로 고칠 게 없다.

배포 환경에서는 CORS가 `WEB_ORIGIN` 목록으로 좁혀진다(개발 모드만 전체 허용).
**Vercel 프리뷰 배포는 URL이 매번 바뀌므로 이 목록에 안 걸려 시세가 막힌다** —
프리뷰까지 쓰려면 그 오리진을 추가하거나 프로덕션 도메인만 쓴다.

**Render 무료 인스턴스는 15분 무요청이면 잠든다.** 다시 깨는 데 1분쯤 걸리고, 그
기동마다 `loadKrxTickers()`가 KRX를 다시 부른다(시장당 1회, 하루 10,000회 제한이라
여유는 있다). 첫 검색이 느린 건 이 때문이지 버그가 아니다. 종가는 기동 시점 기준으로
갱신되므로, 잠들었다 깨면 종가가 최신으로 바뀔 수 있다.

## 아직 없는 것

실시간(장중) 시세 연동, 종가 일일 갱신, 다종목 보유, 데이터베이스, 인증, 테스트 러너, 린터, CI,
네이티브 앱(모바일 웹 다음 단계). 필요해지는 시점에 추가하고 결정 사항을 여기 적는다.
